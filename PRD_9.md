# PRD — 이론가 프로필 (Theorist)

작성일: 2026-08-21
상태: Draft v0.1
상위 문서: 루트 `PRD.md`, 루트 `design.md` (본 문서는 전체 앱 PRD의 하위 feature 문서. 상충되는 내용이 있으면 루트 문서가 우선하며, 이 경우 루트 문서를 함께 업데이트할 것)
연관 문서: `glossary/PRD.md`, `glossary/design.md` (용어집 — 이론가 프로필과 양방향 연동되는 핵심 연관 기능)

---

## 1. 개요

### 1.1 한 줄 정의
용어집과 연결된 사회학 이론가 프로필 아카이브 — 학파별로 훑어보고, 각 인물의 생애·사상·대표 개념을 깊이 있게 읽는 공간.

### 1.2 배경
용어집 상세 화면에는 이미 "관련 이론가" 태그 → 미니 팝업(이름 + 한 줄 소개)까지 구현되어 있고, 팝업 안에 "전체 프로필 보기" 버튼이 비활성 상태로 자리만 마련되어 있다. 이 기능은 그 버튼의 목적지를 만드는 작업이면서, 동시에 인물 중심으로 사회학 이론을 탐색할 수 있는 독립적인 진입점이기도 하다.

### 1.3 목적
- 용어집에서 "이 개념, 누가 만들었지?" 궁금할 때 바로 찾아볼 수 있는 목적지 제공
- 헤더 내비게이션을 통한 독립적인 탐색: 학파별로 이론가를 훑어보며 사회학 사상사의 흐름 파악
- 인물 중심으로 관련 개념(용어)을 역으로 탐색하는 경로 제공

---

## 2. 타겟 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 용어집 이용 중인 학생 | 용어 상세에서 "관련 이론가" 태그 클릭 → 미니 팝업 → "전체 프로필 보기" → 이론가 상세로 이동 |
| 시험/과제 앞둔 학생 | 헤더에서 이론가 메뉴 진입 → 학파 필터로 좁혀보기(또는 생몰년순으로 흐름 파악) → 카드 클릭 → 상세에서 생애·사상 확인 |
| 인물 중심으로 공부하는 학생 | 이론가 상세 페이지 → "관련 용어" 목록에서 이 사람이 만든/발전시킨 개념들을 확인 → 용어집으로 이동 |
| 운영자(유니) | 이론가 프로필을 직접 작성·관리 (학생 제안 없음) |

---

## 3. 범위 (1차 버전)

### 3.1 포함
- 독립 탐색(목록) 페이지 + 상세 페이지, 사이트 헤더 내비게이션에 진입 메뉴
- 목록 화면: 카드/그리드형(인물 사진 중심)
- 학파(카테고리) 필터
- 정렬: 기본은 학파별 그룹 + 자모순, "생몰년순" 토글
- 상세 페이지 정보 카드: 기본 정보(생몰년·국적·소속 학파), 대표 인용구(선택), 북마크 버튼
- 카드 밖 섹션: 생애·사상 요약, 대표 저서/이론, 관련 용어(용어집과 자동 동기화)
- 북마크(로그인 필요, 마이페이지에서 용어 북마크와 함께 확인)
- 용어집 연동: 용어 상세의 "전체 프로필 보기" 버튼 활성화 + 실제 링크 연결
- 콘텐츠 운영: 유니 전용 작성·관리(학생 제안 기능 없음)

### 3.2 제외 (2차 이후 검토)
- 이론가 간 관계(영향을 주고받은 인물) 연결
- 학생의 이론가 제안 기능
- 이론가 프로필에 댓글/토론
- 이론가 수정 이력(버전 관리)
- 다국어(영문 전용 페이지 등)

---

## 4. 기능 요구사항

### 4.1 목록(탐색) 화면
- 카드/그리드형 레이아웃(초상화 중심) — 용어집의 "목록은 리스트로" 원칙과 의도적으로 다른 예외. 사유: 이론가는 "인물"이라는 콘텐츠 특성상 얼굴 이미지로 인지되는 게 자연스럽고, 학생이 시각적으로 훑어보는 경험이 유용하다고 판단해 예외를 둔다.
- 카드에 표시: 초상화(없으면 플레이스홀더), 이름(한글, 영문 병기), 생몰년, 대표 학파 태그 1~2개
- 학파(카테고리) 필터: 다중 선택 허용 여부는 오픈 이슈(용어집과 동일하게 검토)
- 정렬 토글: [학파별 그룹·자모순] ↔ [생몰년순·전체 단일 그리드]
- 검색: 이름(한글/영문) 검색 지원 (용어집 검색과 별도, 이론가 전용 검색창)

### 4.2 이론가 상세

용어집과 동일하게 **카드 영역**과 **카드 밖 영역**으로 나눈다.

**카드 영역** (핵심 정보 — 여기까지만 봐도 "이 사람이 누구인지" 파악 가능해야 함):
- 헤더: 초상화(크게) + 이름(한글, Noto Serif KR) + 영문 원어(Text Secondary) + 생몰년 + 국적
- 학파 태그(해당 이론가가 속한 학파를 배지로 표시, 여러 개 가능)
- 대표 인용구 (선택, 있을 때만 노출 — 카드 안에서 시각적으로 강조되는 위치)
- 북마크 버튼(카드 헤더 영역, 로그인 필요, 비로그인 시 클릭하면 로그인 유도)

**카드 밖 영역**:
- 생애·사상 요약(필수, 긴 본문 — 한 줄 소개보다 깊은 서술)
- 대표 저서/이론(선택, 저서명 + 발표 연도 리스트, 있는 것만 표시)
- 관련 용어(선택, 용어집과 자동 동기화 — 4.4 참고. 칩 형태, 클릭 시 용어집 상세로 이동)

### 4.3 북마크
- 로그인한 사용자만 사용 가능
- 용어 북마크와 동일한 패턴 — 마이페이지에서 "북마크한 용어" / "북마크한 이론가"를 함께(또는 탭으로 구분해) 확인
- 데이터 모델 설계 시 Bookmark를 용어/이론가 공용(타입 구분) 구조로 만들지, 별도 테이블로 분리할지는 5장 참고

### 4.4 용어집 연동 (핵심 통합 지점)

**용어 → 이론가 (기존, 용어집 쪽에 이미 설계됨)**
- 용어 상세의 "관련 이론가" 태그 클릭 → 미니 팝업(이름 + 한 줄 소개) → "전체 프로필 보기" 버튼이 이제 활성화되어 해당 이론가 상세로 이동

**이론가 → 용어 (신규, 본 기능에서 추가)**
- 이론가 상세의 "관련 용어" 섹션은 유니가 별도로 입력하지 않고, 용어 쪽에서 "관련 이론가"로 태깅된 내역을 자동으로 모아서 보여준다(자동 동기화)
- 이를 위해 용어집의 데이터 모델 변경이 필요함: 기존 `Term.related_theorists[].theorist_name`(자유 텍스트) → `theorist_id`(Theorist 레코드 참조)로 전환 제안. 전환 시 `theorist_page_link` 필드는 더 이상 필요 없음(theorist_id로 프로필 URL을 자동 생성)
- 기존에 입력된 용어집 데이터가 있다면 마이그레이션 필요(오픈 이슈)
- 용어 쪽 "한 줄 소개(mini_bio)"는 그 용어 문맥에서의 소개로 유지할지, 이론가 프로필의 정보를 재사용할지는 오픈 이슈

### 4.5 콘텐츠 관리 (유니 전용)
- 별도의 승인 플로우 없음(학생 제안 없이 유니가 직접 작성)
- 초안 상태로 저장해두고 다듬은 뒤 게시하는 흐름이 편하므로 상태값(임시저장/게시) 정도는 두는 것을 제안(5장 참고, 오픈 이슈)

---

## 5. 데이터 모델 (초안)

```
Theorist
- id
- name_ko (string, required)
- name_en (string, optional)
- birth_year (int, optional)
- death_year (int, optional — null이면 생존 중이거나 정보 없음, 구분 방식은 오픈 이슈)
- nationality (string, optional)
- schools (array of string, 학파 태그 — 값 목록은 용어집의 "관점별 정의" 학파명과 동일한 어휘를 쓰는 것을 제안, 오픈 이슈)
- portrait_image_url (string, optional — 없으면 플레이스홀더)
- bio (text, required)                    // 생애·사상 요약, 카드 밖에 노출되는 본문
- key_works (array, optional)
  - title (string)
  - year (int, optional)
- quote (text, optional)
- quote_source (string, optional)
- status (enum: draft / published — 제안, 오픈 이슈)
- created_at, updated_at, published_at

TheoristBookmark (또는 기존 Bookmark 테이블을 target_type으로 확장 — 오픈 이슈)
- user_id
- theorist_id
- created_at
```

**용어집 쪽 변경 제안 (Term 모델)**
```
Term.related_theorists (변경 전)
- theorist_name (string)
- mini_bio (text)
- theorist_page_link (nullable)

Term.related_theorists (변경 후, 제안)
- theorist_id (FK → Theorist.id)
- mini_bio (text, 이 용어 문맥에서의 한 줄 소개 — 유지 여부 오픈 이슈)
```

### 학파(schools) 값 목록 — 초안, 확정 필요
용어집의 "관점별 정의" 섹션에서 이미 쓰이고 있는 학파명과 어휘를 통일하는 것을 제안. 예시 초안:
1. 기능주의
2. 갈등론
3. 상징적 상호작용론
4. 비판이론
5. 페미니즘 사회학
6. 구조주의/구조화이론
7. 현상학적 사회학
8. 후기구조주의/포스트모더니즘
9. 교환이론

> 이 목록은 확정된 것이 아니며, 실제로 프로필을 채워가면서 유니가 조정하는 것을 권장. 용어집 카테고리(9종)처럼 고정값으로 못박을지, 자유롭게 추가 가능한 태그로 둘지도 오픈 이슈.

---

## 6. 의존성

- **계정 시스템**: 북마크 기능은 로그인 필요(용어집과 동일하게 `features/auth`에 의존)
- **용어집 데이터 모델 변경**: 4.4에서 설명한 `theorist_id` 참조 전환이 선행되어야 자동 동기화가 동작함. 이 변경은 용어집 쪽 문서(`glossary/PRD.md` 5장)에도 반영 필요
- **용어집 UI 변경**: 용어 상세의 "전체 프로필 보기" 버튼을 비활성 → 활성으로 전환(`glossary/design.md` 4.6 참고)

---

## 7. 오픈 이슈

- 학파(schools) 값 목록을 고정값으로 할지, 자유 태그로 할지, 그리고 정확한 목록 확정
- 학파 필터 다중 선택 허용 여부
- 생몰년 정보가 불확실하거나 생존 중인 이론가의 death_year 표현 방식
- Bookmark를 용어/이론가 공용 테이블로 만들지, 별도 테이블로 분리할지
- 콘텐츠 상태값(임시저장/게시)을 둘지 여부, 둔다면 임시저장 상태의 프로필이 검색/목록에 노출되지 않도록 처리 필요
- 용어 쪽 `mini_bio`(한 줄 소개)를 이론가 프로필 데이터와 통합할지, 문맥별로 따로 유지할지
- 기존 용어집에 이미 입력된 `related_theorists` 자유 텍스트 데이터가 있다면 `theorist_id` 참조로 마이그레이션하는 방법
- 초상화 저작권: 고전 이론가(마르크스, 뒤르켐, 베버 등)는 대체로 public domain이라 안전하지만, 현대 이론가(부르디외, 기든스, 푸코 등)는 이미지 저작권이 남아있을 수 있어 소스 확인 필요
- 대표 저서 없이 논문/개념 위주로 알려진 이론가의 "대표 저서/이론" 필드를 어떻게 채울지(저서 대신 "핵심 개념"으로 대체 가능하게 할지)
