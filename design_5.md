# design.md — 용어집 (Glossary)

작성일: 2026-08-20
상태: Draft v0.1
상위 문서: 루트 `design.md` (Sociology-whole 디자인 가이드) — 컬러/타이포/컴포넌트 토큰은 전량 상속. 이 문서는 용어집 화면에 한정된 적용 규칙만 정의한다.

---

## 0. 상위 디자인 원칙 적용 요약

루트 design.md에서 용어집에 직접 해당하는 규칙을 다시 확인:

- **레퍼런스형 콘텐츠 원칙**: 용어집은 "레퍼런스형" 콘텐츠로 분류됨 → **Warm Ochre(#C98A4B) 포인트 컬러는 사용하지 않는다.** 활동성 있는 영역(커뮤니티, 퀴즈 등)에만 쓰는 컬러이므로, 용어집은 Forest Green + Navy + 뉴트럴 톤으로만 구성해 학술적 절제감을 유지한다.
- **"목록은 리스트로" 원칙**: 용어 **목록** 화면은 명시적으로 리스트형으로 정의되어 있음 (카드 그리드 나열 금지). 검색바 + 필터 + 리스트 구조는 그대로 유지.
- **용어 상세 내부의 "정의 카드"는 예외**: 목록을 카드로 나열하는 것과는 다른 이야기로, 상세 화면 안에서 용어명·내 정의·원문 정의를 하나로 묶어 보여주는 카드 컨테이너 1개는 사용한다. 이는 루트 design.md 4.3의 카드 스타일 규칙(모서리 8~12px, 옅은 그림자, 얇은 보더)을 그대로 따르며, "여러 개를 격자로 나열하는 카드형 레이아웃"이 아니라 "핵심 정보 하나를 감싸는 컨테이너"로서의 카드다.
- 모서리 반경 8~12px, 그림자는 `0 1px 3px rgba(0,0,0,0.06)` 수준으로 절제.
- 타이틀은 Noto Serif KR, 본문/UI는 Pretendard.

---

## 1. 색상 적용

| 요소 | 컬러 | 비고 |
|---|---|---|
| 페이지 배경 | Off-white `#FAF9F5` | 루트 배경색 그대로 |
| 리스트/상세 패널 배경 | Off-white 또는 Warm Gray `#F1EFE9` (리스트와 상세를 미세하게 구분하고 싶을 때 상세 패널에 Surface 컬러 사용 가능) | |
| 헤더/검색바 강조 | Forest Green (Deep) `#1E4D3B` | 검색 아이콘, 포커스 테두리 등 |
| 카테고리 필터 칩 (선택됨) | Forest Green (Deep) 배경 + 흰 텍스트 | |
| 카테고리 필터 칩 (선택 안 됨) | 투명/Off-white 배경 + Line Gray `#E2E0D8` 테두리 + Text Secondary `#6B6F66` | |
| 정의 카드 배경 | Off-white `#FAF9F5` (페이지와 미세하게 구분하고 싶으면 Surface `#F1EFE9`) | 얇은 보더(Line Gray) + 옅은 그림자로 경계 표시 |
| 정의 카드 보더 | Line Gray `#E2E0D8` | 1px |
| 내 정의 본문 텍스트 | Text Primary (Ink) `#1C1C1A` | 카드 안에서 가장 진하고 큼직하게 |
| 원문 정의 본문 텍스트 | Text Secondary `#6B6F66` | 내 정의보다 옅은 톤으로 보조적 위치임을 시각적으로 표현 |
| 메타 정보(카테고리 라벨, 최근 수정일 등) | Text Secondary `#6B6F66` | |
| 관점별 정의 학파명 라벨 | Navy `#1B2A41` | "학술 콘텐츠 영역" 포인트로 네이비 사용 |
| 구분선 | Line Gray `#E2E0D8` | 리스트 아이템 사이, 카드 밖 섹션 사이 |
| 북마크 아이콘 (활성) | Forest Green (Deep) `#1E4D3B` | 채워진 별/북마크 아이콘 |
| 북마크 아이콘 (비활성) | Text Secondary `#6B6F66`, 라인 아이콘 | |
| 승인 대기 상태 배지 | Text Secondary 톤의 아웃라인 배지 (경고성 오렌지·레드 계열 사용하지 않음 — 절제 유지) | |

> Warm Ochre는 용어집 어디에도 사용하지 않는다. 활동성을 강조하고 싶은 지점(예: "용어 제안하기" 버튼)이 있더라도, Primary(Forest Green) 또는 Secondary(아웃라인) 버튼 스타일로 대체한다.

---

## 2. 타이포그래피 적용

| 요소 | 스타일 |
|---|---|
| 페이지 타이틀 ("용어집") | Noto Serif KR Medium, H1 (22~24px) |
| 용어 상세 헤더 (용어명 한글, 카드 안) | Noto Serif KR Medium, H1~H2 사이 (20~22px 권장) |
| 용어 상세 헤더 (영문 원어, 카드 안) | Pretendard Regular, Caption~Body 사이 크기, Text Secondary 컬러, 한글 용어명 옆 또는 아래 병기 |
| 내 정의 본문 (카드 안, 메인) | Pretendard Medium, Body (15~16px), line-height 1.6 이상, Text Primary — 카드 안에서 가장 먼저 읽히는 텍스트이므로 굵기를 살짝 올림 |
| 원문 정의 라벨 ("원문 정의" 캡션) | Pretendard Medium, Caption (12~13px), Text Secondary |
| 원문 정의 본문 (카드 안, 보조) | Pretendard Regular, Caption~Body 사이 (13~14px), Text Secondary, line-height 1.6 이상 |
| 관점별 정의 학파명 (카드 밖) | Pretendard Medium, Caption~Body |
| 관점별 정의 본문 (카드 밖) | Pretendard Regular, Body |
| 카테고리/필터 칩 텍스트 | Pretendard Medium, Caption (12~13px) |
| 검색바 placeholder | Pretendard Regular, Body |
| 리스트 아이템 용어명 | Pretendard Medium, Body |
| 리스트 아이템 정의 미리보기 | Pretendard Regular, Caption, Text Secondary, 1줄로 말줄임(ellipsis) |

---

## 3. 화면 구조

### 3.1 PC — 리스트+상세 2단 레이아웃
```
┌─────────────────────────────────────────────────────┐
│  검색바 (전체 폭 또는 좌측 패널 폭)                        │
├───────────────────┬───────────────────────────────────┤
│ 카테고리 필터 칩     │                                   │
│ (가로 스크롤 or wrap)│                                   │
├───────────────────┤   ┌───────────────────────────────┐ │
│ [카테고리 그룹 헤더]  │   │ [정의 카드]                     │ │
│  · 용어 A            │   │  용어명 · 영문원어 · 태그 · 북마크│ │
│  · 용어 B            │   │  내 정의 (크게)                 │ │
│ [카테고리 그룹 헤더]  │   │  원문 정의 (작게, 보조)          │ │
│  · 용어 C            │   └───────────────────────────────┘ │
│  ...                │   [관점별 정의 — 카드 밖, 아코디언]    │
│                     │   [관련 이론가 — 카드 밖, 태그+팝업]   │
└───────────────────┴───────────────────────────────────┘
      좌측 패널 (약 320~360px)      우측 패널 (나머지 폭)
```
- 좌측 리스트는 스크롤 시 검색바/필터는 상단 고정(sticky) 권장
- 기본 정렬: 카테고리별 그룹 헤더로 묶고, 그룹 내부는 자모순
- 카테고리 필터를 선택하면 해당 카테고리 그룹만 남기거나, 그룹 헤더 없이 필터링된 리스트만 표시 (택1 — 구현 시 결정, 추천은 필터 선택 시 그룹 헤더 생략하고 단일 리스트로 전환)
- 리스트에서 항목 클릭 시 우측 패널만 갱신 (좌측 스크롤 위치 유지)

### 3.2 모바일 — 목록→상세 전환형
- 목록 화면: 검색바(상단 고정) + 필터 칩(가로 스크롤) + 리스트 (풀스크린)
- 리스트 항목 탭 → 상세 화면으로 전환 (뒤로가기 버튼으로 목록 복귀, 스크롤 위치 유지)
- 상세 화면 내 구성은 PC 우측 패널과 동일한 콘텐츠, 세로로 전체 폭 사용

---

## 4. 컴포넌트 상세

### 4.1 검색바
- 아이콘(라인, 돋보기) + placeholder "용어 또는 정의로 검색"
- 포커스 시 테두리 Forest Green
- 입력 즉시 검색(디바운스) 또는 검색 버튼/엔터 — 추천은 디바운스 즉시 검색

### 4.2 카테고리 필터 칩
- 9종 칩을 가로 스크롤(모바일) 또는 wrap(PC)로 배치
- 선택 시 배경 Forest Green + 흰 텍스트, 미선택 시 아웃라인
- "전체" 칩을 맨 앞에 기본 선택 상태로 배치

### 4.3 용어 리스트 아이템
- 좌측: 용어명(한글, Medium) + 영문 원어(있으면 작게 옆 또는 아래)
- 우측 또는 하단: 내 정의 1줄 미리보기 (ellipsis)
- 우측 끝: 관련 카테고리 중 대표 1개를 작은 텍스트 라벨로 표시 (선택 사항)
- 행 사이 구분선(Line Gray), 리스트 배경은 hover 시 미세한 Surface 톤 배경으로 반응

### 4.4 용어 상세 — 정의 카드

용어 상세의 핵심은 **정의 카드** 하나다. 카드 밖으로 나가는 순간 "더 깊이 파고드는 영역"이라는 걸 시각적으로 분리한다.

**카드 내부 구성 (위에서 아래 순서)**
1. 헤더 행: 용어명(한글, Noto Serif KR) + 영문 원어(Pretendard, Text Secondary, 한글 옆 또는 아래 병기) — 헤더 우측 끝에 북마크 아이콘 버튼 배치 (비로그인 시 클릭하면 로그인 유도 모달/토스트)
2. 카테고리 태그 (해당 용어가 속한 카테고리를 작은 필/아웃라인 배지로, 헤더 바로 아래)
3. **내 정의** — 카드 안에서 가장 크고 진한 텍스트. 별도 라벨 없이 바로 본문 노출 (혹은 "정의" 정도의 작은 캡션만)
4. 구분선(Line Gray, 카드 내부용 — 옅게) 또는 여백으로 분리
5. **원문 정의** — "원문 정의" 캡션 라벨(Text Secondary, Medium) + 본문(Text Secondary, 내 정의보다 작은 크기). 출처가 있으면 본문 옆/아래에 괄호로 작게 표기

카드 스타일: 모서리 8~12px, 보더 Line Gray 1px, 배경 Off-white, 그림자 `0 1px 3px rgba(0,0,0,0.06)` (루트 design.md 카드 규칙 그대로 적용). 패딩은 여유 있게 (24px 내외) — 내 정의를 편하게 읽는 게 최우선 목적이므로 답답하지 않게.

### 4.5 관점별 정의 (카드 밖, 아코디언)
- 정의 카드 **바깥**, 카드 아래에 여백을 두고 별도 섹션으로 배치 (카드와 시각적으로 명확히 분리 — "여기부터는 심화 내용"이라는 신호)
- 섹션 타이틀: "관점별 정의" (H2 스타일, Text Secondary보다는 진하게)
- 각 학파별 정의는 아코디언 아이템으로: 헤더에 학파명(Navy, Medium) + 펼침/접힘 아이콘, 펼치면 정의 본문 + 출처(있으면 Caption으로 하단에)
- 기본 상태는 전체 접힘 — 카드의 내 정의를 먼저 읽게 하고, 필요한 사람만 펼쳐서 더 보게 함
- 이 섹션 자체는 관점별 정의가 없는 용어에서는 통째로 숨김 (빈 섹션 노출 금지)

### 4.6 관련 이론가 (카드 밖, 미니 팝업)
- 관점별 정의 섹션 아래(또는 관점별 정의가 없으면 카드 바로 아래)에 "관련 이론가" 라벨 + 이름 태그(칩 형태, Navy 텍스트 + 아웃라인)
- 태그 클릭 시 작은 팝오버(popover)/툴팁으로 이름 + 한 줄 소개 표시
- 팝오버 하단에 "전체 프로필 보기" 버튼을 배치하되, 이론가 페이지가 없는 현재는 비활성(disabled) 상태 + "준비 중" 텍스트로 처리. 이론가 기능 완성 시 링크만 연결하면 되도록 컴포넌트 구조를 유지할 것
- 이 섹션도 관련 이론가가 없는 용어에서는 통째로 숨김

### 4.7 용어 제안 진입점 & 폼
- 목록 화면 상단 또는 리스트 하단에 "용어 제안하기" 버튼 (Secondary 버튼 스타일, 아웃라인)
- 비로그인 시 클릭하면 로그인 유도
- 폼: 용어명 입력 필드 + 정의 입력 필드(textarea) 두 가지만. 제출 버튼은 Primary 스타일
- 제출 후: "검토 중입니다" 안내 토스트/화면

### 4.8 빈 상태 (Empty state)
- 검색 결과 없음: 리스트 영역에 중앙 정렬로 안내 텍스트("찾으시는 용어가 없어요. 새로운 용어를 제안해보세요") + "용어 제안하기" 버튼 연결
- 상세 패널에 선택된 용어가 없을 때(PC 초기 상태): 중앙 정렬 안내 텍스트("왼쪽에서 용어를 선택해주세요")
- 마이페이지 북마크 목록이 비어있을 때: "아직 북마크한 용어가 없어요" 안내

---

## 5. 반응형 규칙

- Breakpoint는 루트 design.md에 별도 정의가 없으므로 일반적인 기준(768px, 1024px) 사용을 제안 — 루트 문서에 반응형 기준이 추가되면 그에 맞춰 조정
- 1024px 이상: 리스트+상세 2단 레이아웃
- 768px~1023px: 2단 유지하되 좌측 패널 폭을 줄이거나, 상세 패널을 모달/드로어로 전환 (구현 난이도에 따라 선택 — 오픈 이슈)
- 767px 이하: 목록→상세 전환형 (섹션 3.2)

---

## 6. 접근성 고려사항

- 카테고리 필터 칩, 북마크 버튼 등 아이콘만 있는 요소는 스크린리더용 라벨(aria-label) 필수
- 아코디언(관점별 정의)은 키보드로 펼침/접힘 가능해야 함
- 색상만으로 정보를 전달하지 않기 (예: 북마크 상태를 색상뿐 아니라 아이콘 채움 여부로도 구분 — 이미 반영됨)
- 본문 텍스트와 배경 간 명도 대비 WCAG AA 기준 충족 확인 필요 (Text Secondary `#6B6F66` on Off-white `#FAF9F5` 조합은 대비 검증 권장)

---

## 7. 오픈 이슈

- 카테고리 필터를 다중 선택 가능하게 할지, 단일 선택만 허용할지 (PRD 오픈 이슈와 연동)
- 태블릿 구간(768~1023px)에서 2단 레이아웃을 유지할지, 모달형으로 바꿀지
- 리스트 아이템에 카테고리 라벨을 표시할지 여부 (여러 카테고리에 속하면 표시가 복잡해질 수 있음)
- 정의 카드 내부에서 "내 정의"와 "원문 정의"를 구분선으로 나눌지, 여백만으로 나눌지 (둘 다 가능 — 실제 붙여보고 판단 추천)
