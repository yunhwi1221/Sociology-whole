# design.md — 자료실 (Archive)

작성일: 2026-08-22
상태: Draft v0.1
상위 문서: 루트 `design.md`(Sociology-whole 디자인 가이드) — 컬러/타이포/컴포넌트 토큰은 전량 상속. `glossary/design.md`, `theorist/design.md`(리스트/카드 원칙, 접근성 규칙 등 공통 패턴 참고)

---

## 0. 상위 디자인 원칙 적용 요약

- **레퍼런스형 vs 활동형 콘텐츠 원칙**: 자료실은 용어집·이론가와 성격이 다르다. **전체 탐색 탭**(공개 자료를 훑어보는 화면)은 레퍼런스형에 가까워 기존 두 문서와 동일하게 Forest Green + Navy + 뉴트럴 톤만 사용한다. 반면 **내 자료 탭**(업로드, 폴더 생성·관리)은 학생이 직접 무언가를 만들고 정리하는 활동형 화면이다. 다만 일관성을 위해 1차 버전에서는 Warm Ochre를 새로 끌어오지 않고, 주요 액션(자료 올리기, 새 폴더 만들기)도 Forest Green Primary 버튼으로 통일한다 — 용어집이 "용어 제안하기"에도 Warm Ochre 대신 Forest Green을 쓴 것과 같은 판단이다. Warm Ochre 도입 여부는 7장 오픈 이슈로 남긴다.
- **목록은 리스트로 원칙 계승**: 자료실도 목록(폴더 목록, 자료 목록, 전체 탐색 결과)은 리스트형을 기본으로 한다. 이론가처럼 그리드로 예외를 둘 만한 콘텐츠 특성(인물 사진 등)이 없기 때문이다.
- **익명 처리 원칙의 시각적 반영**: 전체 탐색 화면 어디에도 업로더의 이름·닉네임·아바타를 표시하지 않는다. 목록 아이템, 상세 화면 모두 "누가 올렸는지"를 유추할 수 있는 요소를 배제한다.
- **비공개/공개 상태의 명시적 표시**: 학생이 헷갈리지 않도록, "내 자료" 화면의 모든 자료 항목에는 공개 여부를 텍스트+아이콘으로 항상 명시한다(색상만으로 구분하지 않음).
- 모서리 반경 8~12px, 그림자 `0 1px 3px rgba(0,0,0,0.06)`, 타이틀 Noto Serif KR / 본문 Pretendard — 루트 규칙 그대로.

---

## 1. 색상 적용

| 요소 | 컬러 | 비고 |
|---|---|---|
| 페이지 배경 | Off-white `#FAF9F5` | |
| 탭(전체 탐색/내 자료) 선택됨 | Forest Green (Deep) `#1E4D3B` 밑줄 또는 배경 | 용어집 카테고리 칩과 유사한 강조 방식 |
| 탭 선택 안 됨 | Text Secondary `#6B6F66` | |
| 태그 필터 칩(선택됨) | Forest Green (Deep) 배경 + 흰 텍스트 | |
| 태그 필터 칩(선택 안 됨) | 투명 배경 + Line Gray `#E2E0D8` 테두리 + Text Secondary | |
| 자료 리스트 아이템 배경 | Off-white, hover 시 Surface `#F1EFE9` | |
| 폴더 리스트 아이템 배경 | Off-white, 보더 Line Gray 1px | |
| 공개 상태 뱃지("공개") | Forest Green 아웃라인 + Forest Green 텍스트 | |
| 비공개 상태 뱃지("비공개") | Text Secondary 아웃라인 + Text Secondary 텍스트 | 경고성 색상 사용 안 함(단순 상태 표시) |
| 주요 액션 버튼("자료 올리기", "새 폴더") | Forest Green (Deep) 배경 + 흰 텍스트 (Primary 버튼) | |
| 보조 액션 버튼(폴더 이동, 이름 변경 등) | 아웃라인 Line Gray + Text Primary (Secondary 버튼) | |
| 메모 영역 배경(상세, 파일형 전용) | Surface `#F1EFE9` | "이 영역은 나만 본다"는 느낌을 배경 톤 차이로 은은하게 표현 |
| 메모 라벨("나만 보는 메모") | Text Secondary `#6B6F66`, 자물쇠 아이콘 병기 | 비공개 성격을 아이콘으로도 이중 표시 |
| 북마크 아이콘(활성/비활성) | Forest Green (Deep) / Text Secondary | 용어집·이론가와 동일 |
| 삭제 등 위험 액션 | Text Primary(Ink) 텍스트 + 아웃라인, hover 시에만 옅은 경고색 | 평소엔 튀지 않다가 hover 시에만 위험성을 알림 |

> Warm Ochre는 1차 버전에서 사용하지 않는다(0장 참고).

---

## 2. 타이포그래피 적용

| 요소 | 스타일 |
|---|---|
| 페이지 타이틀("자료실") | Noto Serif KR Medium, H1 (22~24px) |
| 탭 라벨("전체 탐색" / "내 자료") | Pretendard Medium, Body |
| 폴더명 | Pretendard Medium, Body |
| 자료 제목(리스트) | Pretendard Medium, Body |
| 자료 제목(상세) | Noto Serif KR Medium, H2 |
| 태그 텍스트 | Pretendard Medium, Caption (12~13px) |
| 업로드일 · 자료 개수 등 메타 정보 | Pretendard Regular, Caption, Text Secondary |
| 텍스트형 자료 본문 | Pretendard Regular, Body (15~16px), line-height 1.6 |
| 메모 본문 | Pretendard Regular, Body~Caption 사이, Text Primary |
| 공개/비공개 뱃지 텍스트 | Pretendard Medium, Caption |

---

## 3. 화면 구조

### 3.1 PC — 전체 탐색 탭

```
┌─────────────────────────────────────────────────────┐
│  [전체 탐색]  [내 자료]                                  │
├─────────────────────────────────────────────────────┤
│  검색바 (자료 제목)                                       │
├─────────────────────────────────────────────────────┤
│  태그 필터 칩 (wrap)              정렬: 최신순 ⌄            │
├─────────────────────────────────────────────────────┤
│  [자료] 제목 · 유형아이콘 · 태그 · 북마크수 · 업로드일          │
│  [자료] ...                                            │
│  [자료] ...                                            │
│  (리스트, 업로더 정보 없음)                                 │
└─────────────────────────────────────────────────────┘
```
- 리스트 아이템 클릭 시 상세 페이지로 이동(별도 URL, 완전한 페이지 전환)

### 3.2 PC — 내 자료 탭

```
┌─────────────────────────────────────────────────────┐
│  [전체 탐색]  [내 자료]                    [+ 자료 올리기]   │
├───────────────────┬───────────────────────────────────┤
│  [+ 새 폴더]        │                                   │
│  📁 미분류 (12)      │   ┌───────────────────────────────┐│
│  📁 사회학개론 (5)    │   │ 선택된 폴더 안 자료 목록            ││
│  📁 조사방법론 (8)    │   │  [자료] 제목 · 공개 뱃지 · 태그      ││
│  ...                │   │  [자료] ...                     ││
│                     │   └───────────────────────────────┘│
└───────────────────┴───────────────────────────────────┘
     좌측: 폴더 리스트           우측: 폴더 안 자료 리스트
```
- 좌측 폴더 리스트에서 폴더를 길게 누르거나 우클릭(또는 ⋯ 메뉴)하면 이름 변경/삭제
- 폴더 삭제 시 "자료는 삭제되지 않고 미분류로 이동합니다" 확인 다이얼로그
- 우측 자료 리스트에서 자료 클릭 시 상세로 이동(또는 우측 패널 갱신 — 구현 시 결정)

### 3.3 상세 페이지

```
┌─────────────────────────────────────────────┐
│ ← 목록으로                                    │
├─────────────────────────────────────────────┤
│  제목                          [공개 ⌄] [북마크]│
│  태그 칩                                       │
├─────────────────────────────────────────────┤
│  (파일형) 미리보기 / 다운로드 버튼                 │
│  (텍스트형) 본문 전체                            │
├─────────────────────────────────────────────┤
│  🔒 나만 보는 메모 (파일형 자료만, 소유자만 보임)     │
│  ┌───────────────────────────────────────┐   │
│  │ 메모 입력/표시 영역 (Surface 배경)          │   │
│  └───────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│  폴더 이동 · 삭제 (소유자에게만 노출)              │
└─────────────────────────────────────────────┘
```
- 전체 탐색을 통해 들어온 공개 자료 상세에서는 메모 영역·폴더 이동·삭제 등 소유자 전용 컨트롤이 보이지 않음(다른 사람 자료를 보는 경우)
- 본인 자료의 상세에서는 공개 여부 토글, 메모, 폴더 이동, 삭제가 모두 노출

### 3.4 모바일
- 상단 탭(전체 탐색/내 자료) 고정
- 전체 탐색: 검색바 상단 고정 + 태그 필터 가로 스크롤 + 리스트
- 내 자료: 폴더 목록 화면 → 폴더 탭 → 그 안 자료 리스트 화면으로 전환(뒤로가기로 폴더 목록 복귀)
- 상세: 전체 화면 전환, 구성은 PC와 동일

---

## 4. 컴포넌트 상세

### 4.1 탭(전체 탐색 / 내 자료)
- 세그먼트 컨트롤 또는 밑줄 탭, 선택 상태는 색상+밑줄 이중 표시(색상만으로 구분 금지)

### 4.2 검색바 & 태그 필터 (전체 탐색)
- placeholder "자료 제목으로 검색"
- 태그 칩은 wrap(PC) / 가로 스크롤(모바일), 선택 시 Forest Green 배경

### 4.3 자료 리스트 아이템
- 유형 아이콘(파일 📄 / 텍스트 📝)으로 한눈에 구분
- 전체 탐색: 제목 + 태그 + 북마크 수 + 업로드일 (업로더 정보 없음)
- 내 자료: 제목 + 태그 + 공개/비공개 뱃지 + 수정일

### 4.4 폴더 리스트 아이템
- 폴더 아이콘 + 폴더명 + 안의 자료 개수(작게, Text Secondary)
- "미분류"는 항상 리스트 최상단 고정, 삭제 불가능한 가상 폴더로 시각적으로도 구분(예: 아이콘 톤 다르게 또는 "시스템" 라벨)

### 4.5 자료 업로드/작성 폼
- 파일형과 텍스트형을 탭 또는 토글로 먼저 선택
- 공통 필드: 제목, 태그, 폴더 선택(드롭다운, 기본값 미분류), 공개 여부 토글(기본 비공개)
- 파일형: 드래그 앤 드롭 업로드 영역 + 파일 선택 버튼
- 텍스트형: 본문 textarea(자동 높이 조절)

### 4.6 공개/비공개 토글
- 상세 화면과 "내 자료" 리스트 양쪽에서 즉시 전환 가능한 스위치형 토글
- 비공개 → 공개 전환 시 "이 자료가 다른 학생에게 공개됩니다"라는 확인 문구(실수 방지)
- 공개 → 비공개 전환은 확인 없이 즉시 처리(안전한 방향이므로)

### 4.7 메모 영역(파일형 자료 상세)
- "🔒 나만 보는 메모" 라벨 + Surface 배경의 인풋 영역
- 자료가 공개 상태여도 이 영역은 소유자 화면에만 나타남(다른 사용자가 보는 상세 화면에는 아예 렌더링하지 않음)

### 4.8 빈 상태(Empty state)
- 전체 탐색 검색/필터 결과 없음: "찾으시는 자료가 없어요"
- 내 자료 — 폴더가 하나도 없을 때: "폴더를 만들어 자료를 정리해보세요" + "새 폴더" 버튼 연결
- 폴더 안에 자료가 없을 때: "아직 이 폴더에 자료가 없어요"
- 마이페이지 자료 북마크가 비어있을 때: "아직 북마크한 자료가 없어요"

---

## 5. 반응형 규칙

- 1024px 이상: 내 자료 탭은 좌(폴더 목록) + 우(자료 목록) 2단 레이아웃
- 767px 이하: 폴더 목록 → 자료 목록 전환형(섹션 3.4)

---

## 6. 접근성 고려사항

- 공개/비공개 상태는 색상뿐 아니라 텍스트 라벨("공개"/"비공개")과 아이콘으로 항상 함께 표시
- 공개 전환 토글은 스크린리더가 현재 상태를 읽을 수 있도록 처리(aria-checked 등)
- 메모 영역은 시각적으로도 "비공개 전용"임을 아이콘+라벨로 명확히 표시해, 실수로 민감한 내용을 공개 자료의 본문에 적지 않도록 유도
- 폴더 삭제 등 되돌리기 어려운 액션은 확인 다이얼로그와 명확한 안내 문구 필수

---

## 7. 오픈 이슈

- Warm Ochre를 "내 자료" 탭(활동형 화면)의 주요 액션에 도입할지, 1차처럼 Forest Green으로 통일할지
- 폴더 목록을 리스트가 아닌 간단한 아이콘 그리드로 보여줄지(파일 탐색기 느낌) — 실제 붙여보고 판단
- 파일형 자료 미리보기를 지원할 형식 범위(이미지/PDF 우선, 나머지는 다운로드만)와 미리보기 UI
- "내 자료"에서 자료 상세를 별도 페이지 전환으로 할지, 우측 패널 갱신형으로 할지(용어집 패턴 참고 가능)
- 공개 전환 확인 문구의 정확한 카피, 그리고 매번 확인받을지 "다시 묻지 않기" 옵션을 둘지
- 자료 리스트에서 파일형/텍스트형을 시각적으로 얼마나 다르게 보여줄지(아이콘만으로 충분한지)
