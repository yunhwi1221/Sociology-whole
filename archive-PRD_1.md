# PRD — 자료실 (Archive)

작성일: 2026-08-22
상태: Draft v0.1
상위 문서: 루트 `PRD.md`, 루트 `design.md` (본 문서는 전체 앱 PRD의 하위 feature 문서. 상충되는 내용이 있으면 루트 문서가 우선하며, 이 경우 루트 문서를 함께 업데이트할 것)
연관 문서: `glossary/PRD.md`, `theorist/PRD.md` (북마크 기능 및 마이페이지 통합 방식 참고), `features/auth/PRD.md` (계정 시스템, 별도 작성 필요)

---

## 1. 개요

### 1.1 한 줄 정의
학생이 자기 학습 자료(파일·노트)를 폴더로 정리해 보관하고, 원하는 자료만 골라 다른 학생과 공유할 수 있는 개인 자료 보관함 겸 공개 아카이브.

### 1.2 배경
용어집·이론가는 유니가 직접 만들고 관리하는 "완성된 레퍼런스"다. 자료실은 반대로 학생 각자가 채워나가는 공간으로, 시험 족보·과제 요약본·수업 필기 같은 자료를 우선 "나를 위해" 정리하고, 그중 도움 될 만한 것만 선택적으로 다른 학생에게 공개하는 흐름을 지원한다. 처음부터 전체 공개 게시판으로 설계하면 개인정보·저작권 부담 때문에 학생들이 업로드를 꺼릴 수 있어, "기본 비공개 + 선택적 공개"를 핵심 원칙으로 잡았다.

논의 초반에 나온 "프로젝트 허브"라는 아이디어는, 실제로는 여러 명이 함께 작업하는 협업 공간이 아니라 개인이 자유롭게 폴더를 만들어 자료를 정리하는 수준이면 충분하다는 결론으로 정리됐다(3.2 제외 항목 참고).

### 1.3 목적
- 개인 학습 자료를 폴더 단위로 정리·보관하는 개인 보관함 제공
- 원하는 자료만 골라 공개해 다른 학생과 공유하는 가벼운 공유 아카이브 기능
- 자료마다 나만 보는 메모를 남겨 개인화된 학습 기록 유지

---

## 2. 타겟 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 자료 정리하는 학생 | 자료실 진입 → 폴더 생성("사회학개론 기말") → 관련 파일 업로드·정리 → 파일마다 개인 메모 남김 |
| 자료 공유하려는 학생 | 잘 정리한 요약노트를 공개로 전환 → 태그 달아서 다른 학생이 찾기 쉽게 함 |
| 자료 찾는 학생 | 헤더 "자료실" → 전체 탐색 탭 → 제목/태그로 검색 → 마음에 드는 자료 북마크 |
| 빠르게 메모만 남기고 싶은 학생 | 파일 없이 텍스트형 자료 작성 → 그 자체로 하나의 자료로 저장(공개 여부 선택 가능) |

---

## 3. 범위 (1차 버전)

### 3.1 포함
- 로그인 사용자: 파일/텍스트형 자료 업로드·작성, 폴더 생성·관리, 자료별 공개/비공개 전환, 파일형 자료에 개인 메모 작성, 북마크
- 비로그인 사용자: 공개 자료 탐색(전체 탐색 탭)만 열람 가능(용어집과 동일 원칙 — 오픈 이슈 참고)
- 자료실 진입: 헤더 내비게이션에 메뉴 추가, 안에 "전체 탐색"(공개 자료) / "내 자료" 두 탭
- 전체 탐색: 제목 검색 + 자유 태그 필터, 공개 자료만 노출, 업로더는 익명 처리
- 내 자료: 폴더 목록 + 폴더 안 자료 목록(파일형/텍스트형 혼재), 미분류 자료함
- 자료 단위 공개/비공개 개별 설정(폴더 단위 아님)
- 콘텐츠 관리: 승인 절차 없는 자유 업로드(용어집·이론가와 다른 운영 방식)

### 3.2 제외 (2차 이후 검토)
- 여러 명이 함께 편집·업로드하는 팀 프로젝트 협업 공간(폴더 정리 수준에서 멈춤 — 논의 결론)
- 댓글/토론, 신고 기능
- 폴더 계층 구조(하위 폴더), 자료의 여러 폴더 동시 소속(자료는 폴더 하나에만 소속, "파일철" 개념)
- 공개 자료에 대한 좋아요/추천, 다운로드 수 표시
- 저장 용량 세부 정책 확정(1차는 러프한 상한만 두고 운영하며 조정)
- 검색 고도화(형태소 분석 등), 텍스트형 자료 본문 전체 검색

---

## 4. 기능 요구사항

### 4.1 진입 구조
- 사이트 헤더에 "자료실" 메뉴 추가
- 진입 시 두 탭 제공: **전체 탐색**(공개 자료 탐색, 기본 탭) / **내 자료**(로그인 필요, 개인 폴더·자료 관리)
- 비로그인 사용자가 "내 자료" 탭 클릭 시 로그인 유도

### 4.2 전체 탐색 (공개 자료)
- 공개로 전환된 자료만 노출(파일형·텍스트형 모두 포함)
- 검색: 자료 제목 기준 키워드 검색
- 필터: 자유 태그(업로더가 자료 등록 시 직접 입력한 태그, 다중 선택 허용 여부는 오픈 이슈)
- 정렬: 기본 최신순, "북마크 많은순" 옵션 추가는 오픈 이슈
- 목록 아이템에 표시: 자료 제목, 자료 유형(파일/텍스트) 아이콘, 태그, 북마크 수, 업로드일 — **업로더 이름/닉네임은 노출하지 않음(익명 처리)**
- 클릭 시 상세로 이동(파일형은 미리보기+다운로드, 텍스트형은 본문 전체 표시)

### 4.3 내 자료
- 폴더 목록 표시(개인이 자유롭게 생성한 폴더, 계층 없이 단일 뎁스)
- "미분류" 기본 폴더(가상 폴더) — 폴더를 지정하지 않은 자료, 그리고 소속 폴더가 삭제된 자료가 모이는 곳
- 폴더 클릭 시 그 안의 자료 목록(파일형/텍스트형 혼재, 최신순 기본 정렬)
- 폴더 생성 / 이름 변경 / 삭제 가능
- 자료를 다른 폴더로 이동 가능
- 각 자료 항목에 공개 여부 표시(뱃지 또는 아이콘), 비공개 ↔ 공개 토글 즉시 전환 가능

### 4.4 자료 업로드 · 작성
- 파일형: 파일 선택 업로드, 제목(필수, 기본값은 파일명), 태그(선택), 폴더 선택(선택, 미지정 시 미분류), 공개 여부(기본 비공개)
- 텍스트형: 제목(필수) + 본문 텍스트 작성, 태그(선택), 폴더 선택(선택), 공개 여부(기본 비공개)
- 파일 형식은 제한을 두지 않고 폭넓게 허용, 용량은 파일당/사용자당 상한만 둠(정확한 수치는 오픈 이슈)
- 업로드·작성에 승인 절차 없음 — 즉시 저장되며, 공개로 설정하면 즉시 전체 탐색에 노출

### 4.5 자료 상세

- **파일형**: 파일 미리보기(지원 형식에 한함) 또는 다운로드 버튼, 제목, 태그, 공개 여부 토글, **메모**(항상 소유자만 보임), 폴더 이동, 삭제
- **텍스트형**: 본문 전체 표시, 제목, 태그, 공개 여부 토글, 폴더 이동, 삭제

> 텍스트형 자료는 별도 메모 필드를 두지 않는다. 본문 자체가 콘텐츠이자 공개 대상이므로 "메모"와 "본문"이 분리될 필요가 없다는 논의 결론이다. 파일형 자료는 파일(공개 가능한 콘텐츠)과 메모(항상 비공개인 개인 주석)의 역할이 명확히 다르므로 두 필드를 분리한다.

### 4.6 메모 (파일형 자료 전용)
- 파일형 자료에만 존재하는 자유 텍스트 필드
- 자료의 공개 여부와 무관하게 **항상 작성자 본인에게만 보임**
- "이 자료가 뭐였더라", "출처 확인 필요" 같은 개인적인 주석 용도

### 4.7 공개 / 비공개 설정
- 자료(파일/텍스트) 단위로 개별 설정하며, 폴더 전체를 한 번에 공개 전환하는 기능은 없음(폴더는 순수 개인 정리 목적)
- 기본값은 항상 비공개 — 업로드·작성 직후 자동으로 공개되지 않음
- 언제든 공개 ↔ 비공개 재전환 가능(공개했다가 다시 비공개로 내릴 수 있음)

### 4.8 북마크
- 로그인 사용자만 사용 가능, 공개 자료에 한해 북마크 가능
- 마이페이지에서 "북마크한 용어" / "북마크한 이론가"와 함께(탭 구분) "북마크한 자료" 확인 — 용어집·이론가와 동일한 패턴 계승

### 4.9 폴더 삭제 정책
- 폴더를 삭제해도 안의 자료는 **삭제되지 않는다** — 폴더 소속만 해제되고 "미분류"로 이동한다
- 폴더 삭제 시 확인 다이얼로그에 "자료는 삭제되지 않고 미분류로 이동합니다" 안내 문구 표시

### 4.10 콘텐츠 관리
- 운영자(유니) 승인 절차 없음 — 학생이 직접 업로드·공개 전환
- 신고·제재 기능은 1차 범위에 포함하지 않음(오픈 이슈 — 향후 부적절한 공개 자료 대응 방안 필요)
- 업로더는 UI에서 익명 처리되지만, 운영 목적상 owner_id는 시스템 내부에 항상 보존(문제 발생 시 추적 가능해야 함)

---

## 5. 데이터 모델 (초안)

```
Folder
- id
- owner_id (FK → User)
- name (string, required)
- created_at, updated_at

Material (자료)
- id
- owner_id (FK → User)
- folder_id (FK → Folder, nullable — null이면 미분류. 폴더 삭제 시에도 자료는 남고 이 값만 null로 전환)
- type (enum: file / text)
- title (string, required)
- file_url (type=file일 때 required)
- file_name (type=file일 때 required)
- file_size (type=file일 때 required)
- file_type (type=file일 때 required, MIME 또는 확장자)
- text_content (type=text일 때 required — 텍스트 자체가 자료 본문)
- memo (text, optional — type=file 자료에만 사용. 항상 owner에게만 노출)
- tags (array of string, optional)
- visibility (enum: private / public, default: private)
- created_at, updated_at

MaterialBookmark
- user_id (FK → User)
- material_id (FK → Material)
- created_at
```

> `Material.owner_id`는 익명 처리 원칙에 따라 UI에는 노출하지 않지만, 데이터로는 항상 유지한다(운영·제재 목적).

---

## 6. 의존성

- **계정 시스템**: 업로드·폴더 관리·메모·북마크는 모두 로그인 필요(`features/auth`에 의존). 공개 자료 탐색만 비로그인 허용 여부는 오픈 이슈
- **파일 스토리지**: 파일형 자료 업로드를 위한 별도 파일 스토리지/CDN 필요(용어집·이론가의 이미지 업로드보다 훨씬 큰 트래픽·용량을 다룸 — 인프라 설계 시 별도 검토 필요)
- **마이페이지**: 북마크한 자료를 용어·이론가 북마크와 함께 표시하려면 마이페이지 쪽 탭 구조 확장 필요

---

## 7. 오픈 이슈

- 저장 용량 정책(1인당 총 용량, 파일당 최대 용량) — 러프하게라도 초기값을 정하고 운영하며 조정 필요
- 보안상 차단해야 할 파일 확장자가 있는지(예: 실행파일 등)
- 비로그인 사용자에게 전체 탐색(공개 자료 열람)을 허용할지 최종 확정
- 태그 필터 다중 선택 허용 여부, 태그 자유 입력 시 오탈자·중복 정리 방안(예: 기존 태그 자동완성 제안)
- 전체 탐색 정렬 옵션에 "북마크 많은순"을 추가할지, 최신순만으로 충분한지
- 텍스트형 자료의 본문 분량 제한(너무 길면 탐색 화면에서 다루기 어려움)
- 파일형 자료의 미리보기 지원 범위(PDF는 미리보기, 그 외 형식은 다운로드만 등 형식별 처리 필요)
- 검색 범위를 제목에서 텍스트형 자료 본문까지 확장할지
- 부적절한 공개 자료(저작권 침해, 개인정보 노출 등)에 대한 신고·제재 기능을 언제 추가할지
- 마이페이지의 "북마크한 자료" 탭에서 자료 유형(파일/텍스트)을 어떻게 구분해 보여줄지
- 익명 처리 원칙의 예외 여부 — 운영자(유니)가 관리 목적으로 업로더를 확인할 수 있는 별도 관리자 화면이 필요한지
