# design.md — 이론가 프로필 (Theorist)

작성일: 2026-08-21
상태: Draft v0.1
상위 문서: 루트 `design.md`(Sociology-whole 디자인 가이드) — 컬러/타이포/컴포넌트 토큰은 전량 상속. `glossary/design.md`(카드 안/밖 분리 패턴, 접근성 규칙 등 공통 패턴 참고)

---

## 0. 상위 디자인 원칙 적용 요약

- **레퍼런스형 콘텐츠 원칙**: 이론가 프로필도 용어집과 마찬가지로 레퍼런스형 콘텐츠 → **Warm Ochre(#C98A4B)는 사용하지 않는다.** Forest Green + Navy + 뉴트럴 톤만으로 구성.
- **"목록은 리스트로" 원칙의 의도적 예외**: 용어집은 이 원칙을 그대로 따르지만, 이론가 목록은 **카드/그리드형**으로 간다. 이론가는 "인물"이라는 콘텐츠 특성상 얼굴 이미지로 인지되는 게 자연스럽고, 학생이 시각적으로 훑어보는 경험이 유용하다고 판단해 예외를 둔다(PRD 4.1 참고). 다만 색상·타이포는 다른 예외 없이 레퍼런스형 원칙을 그대로 따른다 — "시각적으로 화려한 카드"가 아니라 "초상화가 있는 절제된 카드".
- **카드 안/밖 분리 패턴 계승**: 용어집 상세의 "정의 카드 안 = 빠르게 훑기 / 카드 밖 = 깊이 파고들기" 구조를 이론가 상세에도 동일하게 적용.
- 모서리 반경 8~12px, 그림자 `0 1px 3px rgba(0,0,0,0.06)`, 타이틀 Noto Serif KR / 본문 Pretendard — 루트 규칙 그대로.

---

## 1. 색상 적용

| 요소 | 컬러 | 비고 |
|---|---|---|
| 페이지 배경 | Off-white `#FAF9F5` | |
| 이론가 카드(목록) 배경 | Off-white, 보더 Line Gray `#E2E0D8` 1px | |
| 헤더/검색바 강조 | Forest Green (Deep) `#1E4D3B` | |
| 학파 필터 칩(선택됨) | Forest Green (Deep) 배경 + 흰 텍스트 | 용어집 카테고리 칩과 동일 패턴 |
| 학파 필터 칩(선택 안 됨) | 투명 배경 + Line Gray 테두리 + Text Secondary `#6B6F66` | |
| 정보 카드(상세) 배경/보더 | Off-white, Line Gray 1px, 그림자 절제 | 용어집 정의 카드 규칙과 동일 |
| 이름(한글) | Text Primary (Ink) `#1C1C1A` | |
| 영문 원어·생몰년·국적 | Text Secondary `#6B6F66` | |
| 학파 태그(상세 카드 안) | Navy `#1B2A41` 텍스트 + 아웃라인 | 용어집의 "학파명 라벨"과 동일하게 Navy 사용 — 학술 콘텐츠 영역이라는 신호 일관성 유지 |
| 대표 인용구 | Navy `#1B2A41` 또는 Text Primary, 이탤릭 처리로 구분(색보다는 스타일로 구분 권장) | |
| 관련 용어 칩(카드 밖) | Forest Green 또는 Navy 아웃라인 | 용어집의 "관련 이론가" 칩과 시각적으로 짝을 이루도록 동일 톤 사용 |
| 북마크 아이콘(활성/비활성) | Forest Green (Deep) / Text Secondary | 용어집과 동일 |
| 초상화 플레이스홀더(이미지 없을 때) | Surface `#F1EFE9` 배경 + Text Secondary 이니셜 또는 실루엣 아이콘 | |

> Warm Ochre는 여기서도 사용하지 않는다. 초상화 자체가 시각적 무게를 이미 담당하므로, 색으로 활동성을 더할 필요가 없다.

---

## 2. 타이포그래피 적용

| 요소 | 스타일 |
|---|---|
| 페이지 타이틀("이론가") | Noto Serif KR Medium, H1 (22~24px) |
| 상세 헤더(이름, 한글) | Noto Serif KR Medium, H1~H2 (20~22px) |
| 상세 헤더(영문 원어) | Pretendard Regular, Caption~Body, Text Secondary |
| 생몰년·국적 | Pretendard Regular, Caption, Text Secondary |
| 대표 인용구 | Noto Serif KR 또는 Pretendard Medium 이탤릭, Body~H2 사이 크기 — 카드 안에서 시각적으로 도드라지게 |
| 학파 태그 텍스트 | Pretendard Medium, Caption (12~13px) |
| 생애·사상 요약 본문(카드 밖) | Pretendard Regular, Body (15~16px), line-height 1.7 이상(긴 본문이므로 가독성 여유 있게) |
| 대표 저서 목록 | Pretendard Regular, Body |
| 관련 용어 칩 텍스트 | Pretendard Medium, Caption |
| 목록 카드 — 이름 | Pretendard Medium, Body |
| 목록 카드 — 생몰년/학파 | Pretendard Regular, Caption, Text Secondary |

---

## 3. 화면 구조

### 3.1 PC — 목록(그리드) + 상세(별도 페이지 전환)

용어집은 리스트+상세 2단 레이아웃이지만, 이론가는 목록이 그리드형이라 한 화면에 리스트+상세를 같이 두기보다 **목록 페이지 → 클릭 → 상세 페이지로 전환**하는 구조를 권장(그리드+사이드 상세는 카드 크기가 애매해지고 레이아웃이 복잡해짐).

```
[목록 페이지]
┌─────────────────────────────────────────────┐
│ 검색바                                        │
├─────────────────────────────────────────────┤
│ 학파 필터 칩 (wrap)      정렬: 학파순 ⇄ 생몰년순 │
├─────────────────────────────────────────────┤
│ [카드] [카드] [카드] [카드]                     │
│ [카드] [카드] [카드] [카드]     (3~4열 그리드)   │
│ ...                                          │
└─────────────────────────────────────────────┘

[상세 페이지]
┌─────────────────────────────────────────────┐
│ ← 목록으로                                    │
├─────────────────────────────────────────────┤
│ ┌───────────────────────────────────────┐   │
│ │ [정보 카드]                              │   │
│ │  초상화 | 이름·영문·생몰년·국적 · 북마크    │   │
│ │         학파 태그                        │   │
│ │         대표 인용구 (있으면)               │   │
│ └───────────────────────────────────────┘   │
│  생애·사상 요약 (카드 밖)                       │
│  대표 저서/이론 (카드 밖)                       │
│  관련 용어 (카드 밖, 칩)                        │
└─────────────────────────────────────────────┘
```

- 목록 카드 클릭 시 상세 페이지로 라우팅(별도 URL — 용어집처럼 우측 패널만 갱신하는 방식이 아니라 완전한 페이지 전환)
- 목록으로 돌아갈 때 스크롤 위치·필터·정렬 상태 유지

### 3.2 모바일
- 목록: 검색바(상단 고정) + 필터 칩(가로 스크롤) + 정렬 토글 + 카드 그리드 2열
- 카드 탭 → 상세 페이지 전체 화면 전환(뒤로가기로 목록 복귀, 상태 유지)
- 상세 내부 구성은 PC와 동일, 세로 전체 폭

---

## 4. 컴포넌트 상세

### 4.1 검색바
- placeholder "이론가 이름으로 검색"(한글/영문 모두 지원)
- 포커스 시 테두리 Forest Green

### 4.2 학파 필터 칩 + 정렬 토글
- 학파 칩: wrap 배치(PC) / 가로 스크롤(모바일), "전체" 칩 기본 선택
- 정렬 토글: [학파별·자모순] / [생몰년순] 두 상태를 전환하는 세그먼트 컨트롤(버튼 그룹) — 학파 필터가 걸려 있는 상태에서 생몰년순으로 바꾸면, 필터링된 결과 안에서만 생몰년순 정렬(그룹 헤더 없이 단일 그리드로 전환)

### 4.3 이론가 카드(목록)
- 정사각형에 가까운 비율의 초상화(상단, 원형 또는 라운드 사각형 — 통일된 형태 하나로 고정 권장), 이미지 없으면 플레이스홀더(이니셜)
- 하단: 이름(한글, Medium) + 영문(작게), 생몰년, 대표 학파 태그 1~2개(칩)
- hover 시 미세한 그림자 강조(Line Gray 보더 → 살짝 진하게, 또는 그림자 확대) — 클릭 가능함을 시각적으로 알림
- 카드 자체 스타일: 모서리 8~12px, 보더 Line Gray, 그림자 절제(루트 카드 규칙)

### 4.4 상세 — 정보 카드
1. 헤더: 초상화(좌측 또는 상단, 데스크톱은 좌측 배치 권장) + 이름(한글, Noto Serif KR) + 영문 원어 + 생몰년 + 국적 — 헤더 우측 끝에 북마크 버튼
2. 학파 태그(배지, 여러 개 가능)
3. 대표 인용구(있을 때만) — 이탤릭 처리, 카드 안에서 시각적으로 한 번 강조되는 지점. 출처(quote_source)가 있으면 아래 작게 표기
4. 인용구가 없는 이론가는 이 영역 자체를 숨김(빈 자리 노출 금지 — 용어집의 "관점별 정의 없으면 섹션 숨김"과 동일 원칙)

카드 스타일: 루트 카드 규칙 그대로, 패딩 24px 내외.

### 4.5 생애·사상 요약(카드 밖)
- 정보 카드 아래 여백을 두고 별도 섹션, 섹션 타이틀 없이 바로 본문 시작 가능(또는 "생애와 사상" 소제목)
- 긴 본문이므로 줄바꿈/문단 구분이 잘 보이도록 line-height 여유 있게

### 4.6 대표 저서/이론(카드 밖)
- 저서명 + 연도를 리스트로(예: "『자살론』 (1897)")
- 저서 정보가 없는 이론가는 섹션 통째로 숨김

### 4.7 관련 용어(카드 밖)
- "관련 용어" 라벨 + 용어 이름 칩(용어집의 "관련 이론가" 칩과 짝을 이루는 톤)
- 클릭 시 용어집 상세 페이지로 바로 이동(미니 팝업 없이 즉시 이동 — 용어집 쪽은 "이론가 프로필이 없을 수도 있어 팝업 경유"였지만, 여기는 용어집이 이미 존재하는 기능이므로 바로 이동해도 무방)
- 관련 용어가 없는 이론가는 섹션 숨김

### 4.8 빈 상태(Empty state)
- 학파 필터 결과 없음: "해당 학파의 이론가가 아직 없어요" 안내
- 검색 결과 없음: "찾으시는 이론가가 없어요" 안내
- 마이페이지 이론가 북마크 목록이 비어있을 때: "아직 북마크한 이론가가 없어요"

---

## 5. 반응형 규칙

- 1024px 이상: 그리드 4열
- 768px~1023px: 그리드 3열
- 767px 이하: 그리드 2열, 상세는 전체 화면 전환

---

## 6. 접근성 고려사항

- 초상화에 대체텍스트(이름) 필수, 플레이스홀더 이미지도 "초상화 없음" 등 의미 있는 대체텍스트 처리
- 학파 필터 칩, 북마크 버튼 등 아이콘 요소는 aria-label 필수
- 정렬 토글은 현재 선택 상태를 스크린리더가 인지할 수 있도록 처리(aria-pressed 등)
- 색상만으로 정보 전달하지 않기(북마크 상태 등)

---

## 7. 오픈 이슈

- 초상화 형태(원형 vs 라운드 사각형) 최종 확정
- 그리드 목록에서 상세로 이동할 때 완전한 페이지 전환 대신, 모달/드로어로 처리하는 방안도 검토할지(구현 난이도 vs UX)
- 학파 필터와 정렬 토글을 동시에 조작할 때의 UI 배치(한 줄에 다 넣을지, 줄바꿈할지) — 실제 붙여보고 판단
- 인용구 폰트를 Noto Serif KR로 할지 Pretendard 이탤릭으로 할지
