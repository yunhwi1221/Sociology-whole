# PRD — 용어집 (Glossary)

작성일: 2026-08-20
상태: Draft v0.1
상위 문서: 루트 `PRD.md`, 루트 `design.md` (본 문서는 전체 앱 PRD의 하위 feature 문서. 상충되는 내용이 있으면 루트 문서가 우선하며, 이 경우 루트 문서를 함께 업데이트할 것)
연관 문서: `features/auth/PRD.md` (계정 시스템, 별도 작성 필요), `features/theorist/PRD.md` (이론가, 별도 작성 예정)

---

## 1. 개요

### 1.1 한 줄 정의
사회학 전공생이 개념을 찾고, 이해하고, 서로 채워나가는 **살아있는 개념 사전**.

### 1.2 배경
사회학은 법학·의학처럼 "공식 정의"가 하나로 고정된 학문이 아니다. 같은 개념도 이론적 전통(기능주의, 갈등론, 상징적 상호작용론 등)에 따라 다르게 정의될 수 있다. 용어집은 이 특성을 억지로 단순화하지 않으면서도, 시험 대비처럼 빠른 열람이 필요한 상황에서는 부담 없이 쓰일 수 있어야 한다.

### 1.3 목적 (다중 목적 — 사용자에 따라 유연하게)
- 수업/시험 대비: 핵심 개념을 빠르게 찾고 암기
- 과제/논문 작성 참고: 정확한 정의 인용, 학파별 관점 확인, 영문 원어 확인
- 개념 사전: 입문자가 낯선 개념을 처음 접하고 이해

---

## 2. 타겟 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 시험 앞둔 학생 | 검색창에 용어 입력 → 대표 정의만 빠르게 확인 → 다음 용어로 이동 |
| 과제/논문 쓰는 학생 | 용어 검색 → 대표 정의 확인 → "관점별 정의" 펼쳐서 이론적 배경 확인 → 영문 원어 확인 |
| 입문자(신입생 등) | 카테고리로 훑어보며 낯선 개념 탐색 |
| 용어를 추가하고 싶은 학생 (로그인) | 목록에 없는 용어 발견 → "용어 제안하기"로 용어명+정의 제출 |
| 운영자 (유니) | 제안된 용어 검토 → 카테고리/이론가 연결/관점별 정의 등 보강 → 게시 |

---

## 3. 범위 (1차 버전)

### 3.1 포함
- 비로그인 사용자: 열람, 검색, 카테고리 필터
- 로그인(구글) 사용자: 북마크, 용어 제안
- 용어 상세: 대표 정의(필수) + 관점별 정의(선택, 학파별)
- 용어 ↔ 이론가 미니 연결 (이름 + 한 줄 소개 팝업, 이론가 프로필 페이지는 아직 없음)
- 콘텐츠 운영: 학생 제안 → 운영자(유니) 승인 → 게시
- 카테고리 9종 필터

### 3.2 제외 (2차 이후 검토)
- 연관 용어 링크 (정의 안에서 다른 용어로 바로 이동)
- 이론가 전체 프로필 페이지로의 직접 이동 (이론가 기능 완성 후 추가)
- 학교 이메일 인증 (현재는 구글 로그인만)
- 다중 운영자/역할별 권한 체계
- 제안 승인/반려 알림 (이메일/인앱 알림)
- 다크모드 (루트 design.md 기준 초기 범위 밖)
- 용어 수정 이력(버전 관리), 댓글/토론 기능

---

## 4. 기능 요구사항

### 4.1 목록 & 검색 & 필터
- 검색: 용어명(한글/영문) 및 정의 텍스트에서 키워드 매칭. 기본은 용어명 우선 매칭, 결과 없으면 정의 본문까지 확장 검색 (구체 로직은 오픈 이슈 참고)
- 필터: 카테고리 9종 중 선택 (복수 선택 허용 여부는 오픈 이슈)
- 기본 정렬: 카테고리별 그룹핑, 그룹 내부는 자모순
- 목록 아이템에 표시: 용어명(한글), 영문 원어(있는 경우), 내 정의 1줄 미리보기

### 4.2 용어 상세

용어 상세는 **카드 영역**과 **카드 밖 영역**으로 나뉜다.

**카드 영역** (용어의 핵심 정보를 하나의 카드로 감쌈 — 가독성 우선, 여기까지만 봐도 시험 대비가 끝나야 함):
- 용어명(한글 + 영문 병기)
- 카테고리 태그 (해당 용어가 속한 카테고리 표시)
- **내 정의** (유니가 직접 정리한 정의, 필수) — 카드 안에서 가장 먼저, 가장 눈에 띄게 표시되는 정의. 학생이 실제로 읽고 이해/암기하는 메인 정의
- **원문 정의** (출처가 있는 원래 정의, 필수) — "내 정의" 아래에 작게, 보조적으로 표시. 절대 생략하지 않고 항상 함께 노출 (출처 표기 가능)
- 북마크 버튼 (카드 헤더 영역, 로그인 필요, 비로그인 시 클릭하면 로그인 유도)

**카드 밖 영역** (카드 아래에 별도 섹션으로 배치):
- 관점별 정의 (선택) — 학파명 + 그 관점에서의 정의, 아코디언/탭으로 펼쳐보기. 이 섹션은 관점별 정의가 실제 존재하는 용어에만 노출
- 관련 이론가 (선택) — 이름 태그, 클릭 시 미니 팝업(이론가 이름 + 한 줄 소개). 이론가 프로필 페이지가 없는 현재는 팝업 안에 "전체 프로필 보기" 버튼을 비활성 상태로 자리만 마련해두고, 이론가 기능 완성 후 활성화

> 설계 의도: "내 정의"는 유니가 직접 쉽게 풀어쓴 정의로 가장 먼저 읽히게 하고, "원문 정의"는 신뢰도·인용 근거를 위해 계속 남겨두되 시각적으로는 보조적으로 처리한다. 관점별 정의·관련 이론가는 카드 밖에 둬서, 카드 안(빠르게 훑는 용도)과 카드 밖(깊이 파고드는 용도)의 역할을 분리한다.

### 4.3 북마크
- 로그인한 사용자만 사용 가능
- 북마크한 용어 목록은 마이페이지 내에서 확인 (별도의 용어집 내 탭은 만들지 않음)

### 4.4 용어 제안 (로그인 필요)
- 입력 필드: 용어명, 정의 — 두 가지만 (간단하게)
- 카테고리, 이론가 연결, 관점별 정의, 영문 원어 등 나머지는 운영자가 승인 과정에서 직접 채움
- 제출 후 학생에게는 "검토 중" 상태로 안내 (제안한 용어 목록을 학생이 다시 확인할 수 있는 화면은 1차 범위에서는 최소화 — 오픈 이슈 참고)

### 4.5 운영자 승인 (유니 전용)
- 앱 내 관리자 권한은 유니 계정 1개만 존재
- 승인 대기 목록에서 제안된 용어(용어명+정의)를 확인 — 학생이 제출한 정의는 "원문 정의"의 초안 후보로 참고
- 승인 시 아래 필드를 채운 뒤 게시 상태로 전환:
  - 원문 정의 (학생 제출 내용을 다듬거나, 교재·논문 등 출처 있는 정의로 대체/보강, 출처 표기)
  - 내 정의 (유니가 원문 정의를 바탕으로 직접 쉽게 풀어쓴 정의)
  - 카테고리, 관련 이론가, 영문 원어
  - 관점별 정의 (선택, 학파별 관점이 실제 있는 경우에만)
- 반려 시: 목록에서 제거 (반려 사유 전달 방식은 1차 범위 밖 — 오픈 이슈)
- 교수님의 내용 검수는 앱 밖에서(운영자가 별도로 정리해 공유) 진행하며, 앱의 승인 기능과는 별개

---

## 5. 데이터 모델 (초안)

```
Term
- id
- term_ko (string, required)
- term_en (string, optional)
- original_definition (text, required)          // 원문 정의 — 출처가 있는 원래 정의
- original_definition_source (string, optional)  // 원문 정의 출처 (교재/논문명 등)
- curator_definition (text, required)            // 내 정의 — 유니가 직접 정리, 카드에서 가장 먼저 보이는 정의
- perspectives (array, optional)
  - school (string)         예: "기능주의", "갈등론"
  - definition (text)
  - source (string, optional)
- categories (array of enum, 아래 9종 중 선택)
- related_theorists (array, optional)
  - theorist_name (string)
  - mini_bio (text, 1~2문장)
  - theorist_page_link (nullable, 이론가 기능 완성 전까지 null)
- status (enum: pending / published / rejected)
- submitted_by (user_id, nullable — 운영자가 직접 등록한 경우 null)
- created_at, updated_at, published_at

User
- id
- google_id
- email
- name
- role (enum: user / admin) — admin은 유니 계정 1개만

Bookmark
- user_id
- term_id
- created_at
```

### 카테고리 9종 (고정값)
1. 이론/사상
2. 방법론
3. 계급·노동·불평등
4. 젠더·섹슈얼리티
5. 가족·인구
6. 도시·공간
7. 문화·미디어
8. 일탈·범죄
9. 정치·권력·사회변동

---

## 6. 의존성

- **계정 시스템 (구글 로그인)**: 용어집 자체 기능이 아니라 앱 전체가 공유하는 기반 기능. `features/auth/PRD.md`로 별도 작성 필요, 용어집 개발 전 최소 로그인 기능(구글 OAuth, 로그인 상태 확인)은 확보되어야 함
- **이론가 데이터**: 전체 프로필 페이지는 불필요하지만, 최소한 이름 + 한 줄 소개 데이터는 용어집과 함께 준비되어야 미니 팝업이 동작함. 이론가 프로필 페이지(`features/theorist/`)는 이후 별도 진행하되, 완성 시 용어집의 "전체 프로필 보기" 버튼만 연결하면 됨

---

## 7. 오픈 이슈

- 학생이 제안 폼에 입력한 "정의"가 그대로 원문 정의의 초안이 되는 것으로 가정했음 — 이 매핑이 맞는지 확인 필요 (예: 학생 제출 내용은 참고만 하고, 원문 정의는 유니가 항상 별도 출처에서 새로 가져오는 방식이 나을 수도 있음)
- 검색이 정의 본문(내 정의/원문 정의 둘 다? 하나만?)까지 포함할지, 용어명만 검색할지 최종 확정 필요
- 카테고리 복수 선택(태깅) 허용 여부 — 예: "가부장제"가 젠더·섹슈얼리티와 가족·인구 두 카테고리에 동시에 속할 수 있는지
- 학생이 제안한 용어의 처리 현황(대기중/게시됨/반려됨)을 본인이 확인할 수 있는 화면이 필요한지
- 반려 시 사유를 학생에게 전달할지, 전달한다면 어떤 방식으로
- 관점별 정의에서 출처(source) 필드를 필수로 요구할지
- 용어 제안 스팸/어뷰징 방지 (예: 로그인당 하루 제안 횟수 제한)는 1차 범위에 필요한지
