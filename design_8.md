# design.md — 커뮤니티 (Community)

작성일: 2026-08-26 (v0.2 개정)
상태: Draft v0.2
상위 문서: 루트 `design.md`(Sociology-whole 디자인 가이드) — 컬러/타이포/컴포넌트 토큰은 전량 상속. `archive/design.md`(리스트형 원칙, 상태 뱃지 패턴 참고)

---

## 0. 상위 디자인 원칙 적용 요약 — "학과 게시판/공고문"의 감각

`PRD.md` 0장의 톤 앤 매너 원칙을 화면으로 옮기면 다음과 같다. 목표는 에브리타임류 대학 커뮤니티 앱의 "소셜 피드" 문법을 피하고, 학과 게시판·학회 공고문에 가까운 편집적(editorial) 감각을 유지하는 것이다.

- **카드형 피드가 아니라 리스트형 게시판**: 목록은 SNS 피드처럼 카드가 무한히 흘러가는 형태가 아니라, 제목이 한 줄씩 나열되는 전통적인 게시판 리스트로 구성한다(자료실과 동일 원칙).
- **무한 스크롤 대신 페이지네이션**: 스크롤할수록 콘텐츠가 계속 채워지는 피드 경험은 화제성 소비를 유도한다. 목록 하단에 숫자 페이지네이션을 둬서 "다 훑으면 끝"이라는 감각을 준다.
- **채팅 버블이 아니라 문서형 댓글 리스트**: 댓글은 말풍선(버블) UI가 아니라, 발신자·본문이 세로로 나열되는 담백한 텍스트 리스트로 구성한다(게시판 답글 형식).
- **활동형 원칙 + 절제 — Warm Ochre는 최소 지점에만**: 루트 design.md는 Warm Ochre(#C98A4B)를 "커뮤니티·퀴즈 같은 활동형 영역"에 배정해뒀다. 이 앱에서 Warm Ochre를 실제로 쓰는 첫 화면이지만, 화려하게 확장하지 않고 "새 공고 등록" 버튼과 "모집중" 배지 두 곳에만 한정한다. 리스트 배경, 카테고리 탭 등은 다른 기능과 동일하게 뉴트럴 톤을 유지한다.
- **구조화된 정보는 표로**: 스터디·세미나 상세의 모집 정보(대상·인원·기간·일시·장소)는 자유 서술 문단에 섞지 않고, 라벨-값 형태의 정보 표(definition list)로 분리해 "공고문을 읽는" 느낌을 준다.
- **신원 노출의 예외, 그러나 튀지 않게**: 자료실의 "업로더 익명 처리"와 달리 커뮤니티는 닉네임을 노출하지만, 발신자 이름을 강조하는 SNS식 프로필 칩이 아니라 담백한 텍스트 바이라인("닉네임 · 작성일")으로 처리한다.
- 모서리 반경 8~12px, 그림자 `0 1px 3px rgba(0,0,0,0.06)`, 타이틀 Noto Serif KR / 본문 Pretendard — 루트 규칙 그대로. 특히 게시글 제목에는 Noto Serif KR을 다른 기능보다도 일관되게 적용해 "학술 문서를 읽는" 인상을 강화한다.

---

## 1. 색상 적용

| 요소 | 컬러 | 비고 |
|---|---|---|
| 페이지 배경 | Off-white `#FAF9F5` | |
| 카테고리 탭(선택됨) | Forest Green (Deep) `#1E4D3B` 밑줄 또는 배경 | 자료실 탭과 동일 패턴 |
| 카테고리 탭(선택 안 됨) | Text Secondary `#6B6F66` | |
| "공고 등록" 버튼(Primary CTA) | **Warm Ochre `#C98A4B`** 배경 + 흰 텍스트 | 이 앱에서 Warm Ochre가 처음 쓰이는 지점. 단, 헤더 우측의 평범한 버튼으로 배치(FAB·플로팅 버튼 사용 안 함 — 아래 3.3 참고) |
| 모집중 상태 배지 | Warm Ochre 아웃라인 + Warm Ochre 텍스트(배경 채움 아님) | 배경을 꽉 채우기보다 아웃라인으로 절제해, 공고문의 도장/스탬프 느낌에 가깝게 |
| 모집완료 상태 배지 | Text Secondary 아웃라인 + Text Secondary 텍스트 | 자료실 "비공개" 뱃지와 동일한 절제 톤 |
| 카테고리 라벨(스터디/세미나/취업·진로) | Navy `#1B2A41` 텍스트, 아웃라인 배지 | 용어집·이론가의 "학술 라벨" 톤을 계승 |
| 리스트 아이템 배경 | Off-white, hover 시 Surface `#F1EFE9` | |
| 구조화 정보 표(라벨) | Text Secondary `#6B6F66` | "모집 인원", "일시" 등 라벨 텍스트 |
| 구조화 정보 표(값) | Text Primary(Ink) `#1C1C1A` | |
| 작성자 닉네임 | Text Secondary `#6B6F66` | 눈에 띄지 않되 항상 표시, 아바타/프로필 이미지 없이 텍스트만 |
| 댓글 작성자 닉네임 | Text Secondary `#6B6F66` | 본문(Text Primary)과 톤 구분 |
| 댓글 입력창 테두리(포커스) | Forest Green (Deep) | 다른 입력 필드와 통일 |
| 삭제된 댓글 placeholder | Text Secondary, 이탤릭 | "삭제된 댓글입니다" |
| 신고 버튼(평상시) | Text Secondary, 텍스트 링크 형태 | 튀지 않게, hover 시에만 옅은 경고색 |
| 북마크 아이콘(활성/비활성) | Forest Green (Deep) / Text Secondary | 기존 기능과 동일 |

> Warm Ochre는 "공고 등록 버튼"과 "모집중 배지 아웃라인" 두 곳으로 용도를 한정한다. 좋아요 버튼, 인기 태그, 알림 도트처럼 화제성·경쟁을 표시하는 용도로는 절대 확장하지 않는다.

---

## 2. 타이포그래피 적용

| 요소 | 스타일 |
|---|---|
| 페이지 타이틀("커뮤니티") | Noto Serif KR Medium, H1 (22~24px) |
| 카테고리 탭 라벨 | Pretendard Medium, Body |
| 게시글 제목(리스트) | Noto Serif KR Regular, Body~H3 사이 — 다른 기능은 리스트에서 Pretendard를 쓰지만, 공고문 느낌을 강화하기 위해 커뮤니티는 리스트 단계부터 세리프를 쓴다 |
| 게시글 제목(상세) | Noto Serif KR Medium, H2 |
| 구조화 정보 표 라벨 | Pretendard Medium, Caption (12~13px), Text Secondary |
| 구조화 정보 표 값 | Pretendard Regular, Body |
| 게시글 자유 서술 본문 | Pretendard Regular, Body (15~16px), line-height 1.6 |
| 작성자 닉네임 · 작성일 · 댓글 수 | Pretendard Regular, Caption, Text Secondary |
| 댓글 본문 | Pretendard Regular, Body~Caption 사이 (14~15px) |
| 상태 배지 텍스트 | Pretendard Medium, Caption (12~13px) |

---

## 3. 화면 구조

### 3.1 PC — 목록 + 상세 전환형

```
┌─────────────────────────────────────────────┐
│  커뮤니티                        [공고 등록]  │  ← Warm Ochre, 평범한 버튼(FAB 아님)
├─────────────────────────────────────────────┤
│  [전체] [스터디] [세미나] [취업·진로]          │  ← 카테고리 탭
├─────────────────────────────────────────────┤
│  검색바                                       │
├─────────────────────────────────────────────┤
│  [모집중] 스터디 주제 (세리프)  닉네임 · 3일 전 │
│  세미나 주제 (세리프)          닉네임 · 5일 전 │
│  [모집완료] 스터디 주제         닉네임 · 1주 전 │
│  ...                                          │
├─────────────────────────────────────────────┤
│           ‹ 1 2 3 4 5 ›                       │  ← 숫자 페이지네이션(무한 스크롤 아님)
└─────────────────────────────────────────────┘
```

### 3.2 상세 화면

```
┌─────────────────────────────────────────────┐
│  [스터디] [모집중]                            │
│  게시글 제목 (Noto Serif KR, 크게)            │
│  닉네임 · 작성일 · 북마크 · 신고               │
├─────────────────────────────────────────────┤
│  모집 대상   전공 무관, 1~4학년               │  ← 구조화 정보 표(라벨-값)
│  모집 인원   4명                              │
│  진행 방식   오프라인 (스터디룸)               │
│  진행 기간   9월~12월, 주 1회                 │
├─────────────────────────────────────────────┤
│  자유 서술 본문(진행 목표·커리큘럼 개요 등)     │
├─────────────────────────────────────────────┤
│  댓글 3                                       │
│  닉네임   본문                     · 신고      │
│  닉네임   삭제된 댓글입니다                    │
│  ...                                          │
│  [댓글 입력창]                                │
└─────────────────────────────────────────────┘
```

### 3.3 모바일 — 목록→상세 전환형
- 목록: 카테고리 탭(가로 스크롤) + 검색바(상단 고정) + 리스트(풀스크린) + 하단 페이지네이션
- **"공고 등록" 버튼은 FAB(플로팅 버튼)로 두지 않는다** — 헤더 우측 고정 버튼으로 배치해, 다른 기능(자료실의 "자료 올리기")과 동일한 위치·형태를 유지한다. FAB는 소셜 앱 특유의 "가볍게 툭 던지는 글쓰기"를 연상시키므로 의도적으로 피한다.
- 상세: 세로 전체 폭, 댓글 입력창은 화면 하단 고정

---

## 4. 컴포넌트 상세

### 4.1 카테고리 탭
- 전체/스터디/세미나/취업·진로 4개(전체 포함), 선택 시 Forest Green 밑줄+텍스트

### 4.2 구조화 정보 표 (상세 화면)
- 스터디: 모집 대상 / 모집 인원 / 진행 방식 / 진행 기간 / 지원 방법
- 세미나: 발표자·주최 / 일시 / 장소 / 참가 대상 / 신청 방법
- 취업·진로: 정보 유형 / 기관명 / 마감일 / 지원 링크
- 라벨-값 2열 표 형태, 값이 없는 항목(선택 필드 미입력)은 행 자체를 숨김

### 4.3 상태 배지
- 모집중: Warm Ochre 아웃라인, "모집중" 라벨
- 모집완료: Text Secondary 아웃라인, "모집완료" 라벨
- 취업·진로 카테고리 글에는 상태 배지 없음(모집 개념이 없으므로)

### 4.4 게시글 등록 폼
- 카테고리 선택(필수) → 카테고리에 따라 폼 항목 전체가 전환됨(PRD 4.3의 구조화 필드)
- 자유 서술 항목(진행 목표, 상세 설명)만 자동 높이 조절 textarea, 나머지는 정해진 입력 형식(날짜 선택, 드롭다운 등)을 사용해 항목을 빠짐없이 채우도록 유도

### 4.5 댓글 영역
- 발신자 텍스트 + 본문이 세로로 나열되는 리스트 형태(말풍선·배경색 구분 없음)
- 각 댓글에 닉네임 + 본문 + 신고 텍스트 링크, 본인 댓글에는 삭제 링크도 표시
- 삭제된 댓글은 "삭제된 댓글입니다"로 자리만 유지

### 4.6 신고
- 게시글 상세 우측 상단에 텍스트 링크로 배치("신고"), 댓글은 각 댓글 우측에 작게
- 클릭 시 사유 선택 모달(잘못된 정보/부적절한 콘텐츠/스팸·광고/기타)

### 4.7 빈 상태(Empty state)
- 카테고리 내 글이 없을 때: "아직 등록된 공고가 없어요" + 등록 버튼 연결
- 검색 결과 없음: "검색 결과가 없어요"
- 댓글이 없을 때: "가장 먼저 댓글을 남겨보세요"

---

## 5. 반응형 규칙

- 767px 이하: 카테고리 탭 가로 스크롤, "공고 등록" 버튼은 헤더 우측 고정(FAB 아님, 3.3 참고)
- 1024px 이상: 목록·상세 모두 본문형 리스트가 지나치게 넓어지지 않도록 최대 폭 제한(가독성 우선)

---

## 6. 접근성 고려사항

- 모집중/모집완료 상태는 색상뿐 아니라 텍스트 라벨로 항상 함께 표시
- 신고 링크는 스크린리더가 "신고하기"로 읽도록 명확한 텍스트 사용(아이콘 단독 사용 안 함)
- 댓글 삭제 확인은 다이얼로그로 한 번 더 확인(되돌릴 수 없는 액션)
- 닉네임 미설정 사용자가 등록 버튼을 눌렀을 때, 닉네임 설정으로 안내하는 흐름이 스크린리더에서도 명확히 전달되도록 처리
- 구조화 정보 표는 시맨틱하게 `<dl>`(definition list) 또는 테이블 마크업으로 구성해 스크린리더가 라벨-값 관계를 읽을 수 있게 한다

---

## 7. 오픈 이슈

- 게시글 제목에 세리프(Noto Serif KR)를 리스트 단계부터 쓰는 것이 실제로 가독성·정보 밀도 면에서 괜찮은지 프로토타입으로 검증 필요(다른 기능은 리스트에서 Pretendard만 사용)
- 페이지네이션 페이지당 게시글 수(예: 20개)를 얼마로 정할지
- 구조화 정보 표에서 선택 필드가 대부분 비어있는 글이 많을 경우(예: 취업 정보에 지원 링크 없음) 표가 지나치게 짧아 보이는 문제를 어떻게 다듬을지
