# PRD — 커뮤니티 (Community)

작성일: 2026-08-26 (v0.2 개정)
상태: Draft v0.2
상위 문서: 루트 `PRD.md`, 루트 `design.md` (본 문서는 전체 앱 PRD의 하위 feature 문서. 상충되는 내용이 있으면 루트 문서가 우선하며, 이 경우 루트 문서를 함께 업데이트할 것)
연관 문서: `archive/PRD.md`(익명 처리 원칙과의 차이점 비교), `admin/PRD.md`(신고 관리 22장 — 본 기능이 실제 구현되면서 관리자 P1 항목이 P0로 승격될 가능성)

---

## 0. 톤 앤 매너 원칙 — "에브리타임처럼 만들지 않는다"

이 기능은 학생 간 소통이 처음 생기는 지점이라, 방향을 잘못 잡으면 가장 쉽게 에브리타임류의 자유 게시판 문화(익명 잡담, 인기글 경쟁, 화제성 콘텐츠 소비)로 흘러간다. Sociology Whole은 용어집·이론가처럼 절제된 학술 레퍼런스 톤을 앱 전체의 기본값으로 삼아왔으므로, 커뮤니티도 "학과 게시판/학회 공고"에 가까운 감각을 유지한다.

### 하지 않는 것
- 목적 없는 잡담 게시판(자유게시판) — 스터디·세미나·취업·진로처럼 명확한 목적이 있는 글만 존재한다
- 좋아요/추천수, "인기글" 노출, 실시간 랭킹 — 화제성으로 콘텐츠가 소비되는 구조를 만들지 않는다
- 완전 익명 글쓰기 — 책임 소재 없는 저격·뒷담화가 자랄 토양을 주지 않는다(단, 실명은 아니고 닉네임까지만 — 4.5 참고)
- 채팅 버블 UI, 이모지 리액션, 무한 스크롤 피드 — 소셜 앱의 문법이 아니라 게시판/공고문의 문법을 따른다

### 대신 지향하는 것
- 글쓰기는 자유 서술이 아니라 **카테고리별로 정해진 항목을 채우는 구조화된 등록 폼**을 사용한다(공고문에 가까운 형식 — 4.3 참고)
- 목록은 최신순 리스트 + 페이지네이션(무한 스크롤 아님), 상세는 정보를 표 형태로 정리해 한눈에 훑을 수 있게 한다
- 타이포그래피·색상은 루트 design.md의 절제된 톤(Noto Serif KR 타이틀, 뉴트럴 배경)을 그대로 따르고, 활동성을 표시할 때도 최소한의 지점에만 색을 쓴다(design.md 0장)

---

## 1. 개요

### 1.1 한 줄 정의
사회학과 학생들이 스터디·세미나 모집, 취업·진로 정보를 공고문 형식으로 등록하고 나누는 게시판.

### 1.2 배경
용어집·이론가·자료실은 모두 "콘텐츠를 정리·보관·열람"하는 성격의 기능이었다. 자료실조차 업로더를 익명 처리하고 댓글·토론 기능을 의도적으로 배제했을 만큼, 지금까지 Sociology Whole은 학생 간 직접적인 소통이 없는 앱이었다.

커뮤니티는 이 앱에서 처음으로 "학생이 학생에게 말을 거는" 기능이다. 스터디 모집은 지원자가 글쓴이에게 연락할 수 있어야 하고, 세미나 모집은 문의를 받아야 하고, 취업·진로 정보는 서로 정보를 주고받는 게 핵심이다. 그렇다고 해서 익숙한 "대학 커뮤니티 앱"의 문법(자유게시판, 인기글, 익명성)을 그대로 가져오지는 않는다 — 0장의 원칙대로, 목적성 있는 게시판이자 학과 공고문에 가까운 톤을 유지한다.

### 1.3 목적
- 스터디·세미나 모집자와 참여 희망자를 연결
- 취업·진로 관련 정보(채용, 인턴, 대학원 진학 등)를 학생들이 서로 공유
- 목적 없는 잡담·화제성 콘텐츠로 흐르지 않는, 신뢰할 수 있는 학과 게시판 제공

---

## 2. 타겟 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 스터디 모집하는 학생 | 커뮤니티 진입 → 등록(카테고리: 스터디) → 주제·목표·진행 방식·모집 인원 등 정해진 항목을 채워 공고 등록 → 댓글로 지원 받음 → 인원이 차면 모집 상태를 "모집완료"로 직접 전환 |
| 세미나를 공지하는 학생/조교 | 등록(카테고리: 세미나) → 주제·발표자·일시·장소·신청 방법 항목 작성 → 댓글로 문의 응대 |
| 취업·진로 정보를 찾는 학생 | 카테고리 "취업·진로"에서 채용공고·인턴·대학원 정보를 목록에서 훑어보고 키워드 검색 |
| 부적절한 글을 본 학생 | 게시글 또는 댓글의 신고 버튼 클릭 → 사유 선택 → 접수 |
| 운영자(유니) | 신고된 글/댓글 검토 → 처리(삭제 또는 반려) — `admin/PRD.md` 22장 신고 관리 연동 |

---

## 3. 범위 (1차 버전)

### 3.1 포함
- 카테고리: **스터디 / 세미나 / 취업·진로** 3종. 자유게시판(잡담)은 만들지 않는다(0장 원칙)
- 게시글 등록: 자유 서술형 제목+본문이 아니라, **카테고리별 구조화된 폼**(4.3 참고)으로 등록한다
- 목록: 카테고리 탭 + 리스트형(자료실과 동일 원칙, 그리드 아님) + 페이지네이션, 제목·본문 키워드 검색
- 상세: 구조화된 정보(모집 대상·인원·기간 등)를 표 형태로 정리해 노출 + 자유 서술 본문 + 댓글
- 댓글: 게시글당 댓글 목록 + 작성(대댓글 없이 단일 depth) — 지원·문의 목적의 실용적 소통 창구로 위치시킨다
- 작성자 표시: **닉네임 공개** (실명 아님, 완전 익명도 아님 — 4.5 참고)
- 신고: 게시글·댓글 각각 신고 가능, 사유 선택형(잘못된 정보/부적절한 콘텐츠/스팸·광고/기타), 관리자 처리 상태와 연동
- 게시글/댓글 삭제: 작성자 본인 삭제 가능(댓글은 소프트 삭제 — "삭제된 댓글입니다" 표시로 스레드 흐름 유지)
- 북마크: 기존 기능(용어·이론가·자료)과 동일 패턴으로 게시글 북마크 지원

### 3.2 제외 (2차 이후 검토)
- 대댓글(중첩 댓글) — 1차는 플랫 댓글만
- 쪽지·1:1 채팅(DM)
- 댓글·모집 알림(푸시/이메일)
- 모집 인원 도달 시 자동 마감 — 1차는 작성자가 수동으로 "모집완료" 전환
- 게시글/댓글 수정 이력 관리

### 3.3 앱 차원에서 영구적으로 만들지 않는 것 (2차 검토 대상이 아니라 원칙적 제외)
- 자유게시판(잡담), 좋아요/추천수, 인기글·실시간 랭킹, 완전 익명 글쓰기, 이모지 리액션 — 0장 참고. 이 항목들은 "아직 안 만든 것"이 아니라 "이 앱의 성격상 만들지 않기로 한 것"이다.

---

## 4. 기능 요구사항

### 4.1 진입 구조
- 사이트 헤더에 "커뮤니티" 메뉴 추가
- 진입 시 카테고리 탭(스터디/세미나/취업·진로) + 통합 목록, 기본 탭은 "전체"

### 4.2 목록
- 리스트형(용어집·자료실과 동일 원칙): 제목, 카테고리 라벨, 모집 상태 배지(해당 카테고리인 경우), 작성자 닉네임, 댓글 수, 작성일
- 검색: 제목+본문 키워드
- 정렬: 최신순만 지원(인기순·조회순 없음 — 0장 원칙)
- 목록 하단은 페이지네이션(숫자 기반)을 사용한다 — 무한 스크롤은 피드형 소비를 유도하므로 쓰지 않는다

### 4.3 게시글 등록 — 카테고리별 구조화된 폼

자유 서술형 글쓰기 대신, 카테고리마다 정해진 항목을 입력하는 폼을 사용한다. 학회·학과 공고문을 등록하는 감각에 가깝게 설계한다.

**스터디**
- 스터디 주제(필수)
- 진행 목표/커리큘럼 개요(필수, 자유 서술)
- 진행 방식(온라인/오프라인/혼합)
- 모집 대상(학년·전공 제한 여부, 자유 서술)
- 모집 인원(선택)
- 진행 기간 및 모임 주기(예: "9월~12월, 주 1회")
- 지원 방법(댓글/외부 링크 등, 자유 서술)
- 모집 상태(기본값 "모집중", 작성자가 "모집완료"로 전환 가능)

**세미나**
- 세미나 주제(필수)
- 발표자/주최(필수)
- 일시(필수)
- 장소(온라인 링크 또는 오프라인 장소, 필수)
- 참가 대상(자유 서술)
- 신청 방법(필수, 자유 서술 또는 외부 링크)
- 모집 상태(기본값 "모집중")

**취업·진로**
- 정보 유형(채용/인턴/대학원 진학/기타)
- 기관명/프로그램명(필수)
- 마감일(선택)
- 지원 링크(선택)
- 상세 설명(필수, 자유 서술)
- 모집 상태 배지 없음(공고 성격이 아닌 정보 공유이므로)

> 자유 서술 항목(진행 목표, 상세 설명 등)은 남겨두되, 항목 자체를 구조화해 "언제·누가·무엇을·어떻게"가 항상 채워지도록 한다. 이는 캐주얼한 잡담형 게시글과 구분되는 핵심 장치다.

### 4.4 상세 & 댓글
- 상세: 제목, 구조화된 정보(표 형태로 정리 — design.md 4.2 참고), 자유 서술 본문, 작성자 닉네임, 작성일, 북마크, 신고 버튼
- 댓글: 목록(작성 순), 댓글 작성창(로그인 필요), 본인 댓글 삭제(소프트 삭제)
- 작성자는 자신의 글에 한해 모집 상태를 "모집완료"로 전환 가능(상세 화면 상단에 노출된 토글)

### 4.5 작성자 정보 노출 (닉네임 공개 원칙)
- 자료실의 "완전 익명" 원칙과 달리, 커뮤니티는 **닉네임을 공개**한다. 스터디·세미나 모집의 목적상 "누가 쓴 글인지" 최소한의 신뢰 정보가 필요하기 때문.
- 실명은 노출하지 않는다(학교 커뮤니티 특성상 실명 노출은 원치 않는 학생이 있을 수 있음).
- 닉네임은 마이페이지에서 설정 — **이 기능은 지금까지 앱에 없었으므로 신규로 필요**(6장 의존성 참고). 닉네임 미설정 사용자는 커뮤니티 글쓰기 전 닉네임 설정을 먼저 요구.

### 4.6 신고
- 게시글/댓글 각각에 신고 버튼
- 사유: 잘못된 정보 / 부적절한 콘텐츠 / 스팸·광고 / 기타(자유 서술)
- 처리 상태: 접수 → 검토 중 → 처리 완료 (`admin/PRD.md` 22장과 동일한 상태 모델)
- 신고 처리는 관리자 전용, 신고자 정보는 관리자에게만 노출

### 4.7 북마크
- 로그인 사용자만 사용 가능
- 마이페이지 "북마크한 글" 탭에서 용어·이론가·자료 북마크와 함께 확인(기존 패턴 계승)

### 4.8 콘텐츠 관리
- 승인 절차 없음 — 자료실과 동일하게 즉시 게시(스터디 모집 특성상 승인 대기는 실효성이 없음)
- 신고 접수 시에만 관리자 검토 개입

---

## 5. 데이터 모델 (초안)

```
Post
- id
- author_id (FK → User)
- category (enum: study / seminar / career)
- title (string, required)
- content (text, required — 자유 서술 부분)
- recruit_status (enum: open / closed, nullable — study/seminar에서만 사용)

# study 카테고리 전용 구조화 필드
- study_goal (text, nullable)
- study_format (enum: online / offline / hybrid, nullable)
- study_target (text, nullable)
- recruit_count (integer, nullable)
- study_period (string, nullable — 자유 서술: "9월~12월, 주 1회")
- apply_method (text, nullable)

# seminar 카테고리 전용 구조화 필드
- speaker_host (string, nullable)
- event_datetime (datetime, nullable)
- location (string, nullable)
- target_audience (text, nullable)

# career 카테고리 전용 구조화 필드
- info_type (enum: job / internship / grad_school / other, nullable)
- organization (string, nullable)
- deadline (date, nullable)
- apply_link (string, nullable)

- view_count (integer, default 0)
- created_at, updated_at

Comment
- id
- post_id (FK → Post)
- author_id (FK → User)
- content (text, required)
- deleted_at (nullable — soft delete, "삭제된 댓글입니다" 표시용)
- created_at

Report
- id
- target_type (enum: post / comment)
- target_id
- reporter_id (FK → User)
- reason (enum: wrong_info / inappropriate / spam / other)
- detail (text, optional — reason=other일 때)
- status (enum: received / reviewing / resolved)
- created_at, resolved_at

PostBookmark
- user_id (FK → User)
- post_id (FK → Post)
- created_at
```

> 카테고리 전용 필드를 `Post` 테이블에 전부 두는 것은 1차 구현 단순화를 위한 선택이다. 카테고리가 늘어나면 `PostDetail`류의 별도 테이블로 분리하는 편이 나을 수 있음(7장 오픈 이슈).

> `Comment`는 완전 삭제 대신 소프트 삭제를 사용해, 댓글 스레드 중간이 비어 보이지 않게 한다.

---

## 6. 의존성

- **계정 시스템 + 닉네임 필드**: 지금까지 이 앱은 닉네임 개념이 없었다. 커뮤니티 글쓰기 전 마이페이지에서 닉네임을 설정하도록 하는 온보딩 플로우가 필요 — `features/auth/PRD.md` 또는 마이페이지 PRD 업데이트 필요
- **관리자 신고 관리**: `admin/PRD.md` 22장은 "신고 기능이 실제 서비스에 구현된 경우" 관리자 메뉴에 포함한다고 명시되어 있음 — 커뮤니티 출시 시점에 이 항목을 P1에서 P0로 승격해 함께 구현 필요
- **마이페이지**: "북마크한 글" 탭 추가, 닉네임 설정 UI 추가

---

## 7. 오픈 이슈

- 카테고리별 구조화 필드를 `Post`에 전부 둘지, 카테고리 확장을 고려해 별도 테이블로 분리할지
- 닉네임 변경을 자유롭게 허용할지, 허용한다면 과거 작성 글의 작성자 표시가 이전 닉네임/현재 닉네임 중 무엇을 따를지
- 닉네임 옆에 학년·전공 등 소속 정보를 함께 표시할지(신뢰도를 높이되 개인정보 노출 범위가 넓어짐 — 트레이드오프 검토 필요)
- 모집 상태 "모집완료" 수동 전환만으로 충분한지, 모집 인원 도달 시 자동 마감이 필요한지
- 게시글/댓글 수정 기능을 1차에 넣을지(현재 범위는 삭제만 가능, 수정 불가로 가정)
- 신고 사유 "기타" 선택 시 자유 서술 글자 수 제한
- 취업·진로 카테고리의 정보 신뢰성 검증 방안(예: 외부 채용 링크가 만료되거나 사기성 공고일 가능성) — 신고 기능 외 별도 장치가 필요한지
- 카테고리별 목록을 하나의 통합 리스트로 볼지, 카테고리 진입 시 완전히 분리된 화면으로 볼지
