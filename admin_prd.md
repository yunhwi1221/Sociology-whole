# PRD — 관리자창 (Admin)

작성일: 2026-08-26
상태: Draft v0.1
상위 문서: 루트 `PRD.md`, 루트 `design.md` (본 문서는 전체 앱 PRD의 하위 feature 문서. 상충되는 내용이 있으면 루트 문서가 우선하며, 이 경우 루트 문서를 함께 업데이트할 것)
연관 문서: `glossary/PRD.md`(4.5 운영자 승인, 5장 User.role), `theorist/PRD.md`(4.5 콘텐츠 관리, 5장 status), `archive/PRD.md`(4.10 콘텐츠 관리, 7장 오픈 이슈 — 관리자 화면 필요성 질의)

---

## 1. 개요

### 1.1 한 줄 정의
윤휘(운영자) 한 명이 용어 승인, 이론가 프로필 관리, 자료실 모더레이션, 공지사항 게시, 학생 문의 응답을 한 곳에서 처리하는 통합 관리자 전용 영역.

### 1.2 배경
용어집·이론가·자료실 세 PRD에는 각각 "운영자가 해야 할 일"이 이미 명시돼 있었다 — 용어집의 승인 대기 목록 처리(4.5), 이론가의 직접 작성·관리(4.5), 자료실의 업로더 확인·모더레이션 필요성(오픈 이슈). 그런데 이 일들을 수행할 화면이 어디에 있는지는 어느 문서에도 정의돼 있지 않았다. 기능마다 관리 화면을 따로 만들면 유니가 여러 화면을 오가야 하고, 앱이 커질수록 관리 기능이 파편화된다. 이 문서는 세 PRD에 흩어진 관리 요구사항을 하나의 관리자창으로 통합하고, 여기에 공지사항 게시와 학생 문의 응답이라는 두 신규 기능을 추가로 정의한다.

### 1.3 목적
- 용어집·이론가·자료실에 흩어진 관리 작업을 한 곳(`/admin`)에서 처리
- 유니가 전체 학생에게 공지사항을 게시할 수 있는 창구 제공
- 학생이 남긴 문의(질문/신고/제안 등)를 확인하고 개별 답변할 수 있는 창구 제공
- 앱 전체 관리자 권한은 유니 계정 1개로 한정 (glossary/PRD.md의 `User.role` 원칙을 앱 전역 원칙으로 승격)

---

## 2. 타겟 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 운영자(유니) — 용어 승인 | 관리자창 진입 → 용어집 승인 대기 목록 → 항목 클릭 → 원문 정의·내 정의·카테고리·관련 이론가 입력 → 게시 |
| 운영자(유니) — 이론가 관리 | 관리자창 진입 → 이론가 목록 → 새 프로필 작성 또는 기존 프로필 수정 → 임시저장/게시 |
| 운영자(유니) — 자료실 모더레이션 | 문의함(신고)으로 문제 자료 인지 → 관리자창에서 실제 업로더 확인 → 자료 비공개 전환 또는 삭제 |
| 운영자(유니) — 공지사항 | 관리자창 → 공지사항 작성 → 전체 학생에게 노출 |
| 운영자(유니) — 문의 응답 | 관리자창 → 문의함 → 대기중 문의 확인 → 답변 작성 → 학생에게 전달 |
| 학생 — 문의 남기기 | 헤더 또는 마이페이지의 "문의하기" 진입 → 유형 선택(질문/신고/제안) → 내용 작성 → 제출 |
| 학생 — 공지사항 확인 | 헤더 또는 홈에서 공지사항 배너/목록 확인 |

---

## 3. 범위 (1차 버전)

### 3.1 포함
- 관리자 전용 진입: `role=admin` 계정만 접근 가능한 `/admin` 영역. 일반 학생 계정은 URL을 알아도 접근 불가(라우트 가드), 비로그인 시 로그인 페이지로 리다이렉트
- **용어집 승인 대기** 목록 + 승인/반려 처리 화면 (glossary/PRD.md 4.5 이관 — 신규 기능 아님, 화면 위치를 관리자창으로 확정)
- **이론가 프로필** 목록 + 작성/수정/삭제 화면, 임시저장(draft)/게시(published) 상태 전환 (theorist/PRD.md 4.5, 5장 이관)
- **자료실 모더레이션**: 문제로 지목된 자료를 검색/조회해 실제 업로더(owner_id) 확인, 자료 비공개 전환 또는 삭제 (archive/PRD.md 오픈 이슈 이관)
- **공지사항**: 작성/수정/삭제, 전체 학생에게 노출되는 목록
- **문의함**: 학생이 남긴 문의 목록(최신순), 상태(대기중/답변완료) 표시, 문의별 답변 작성 → 학생에게 전달
- **학생용 문의하기** 진입점(폼): 유형(질문/신고/제안 등) + 내용 입력, 로그인 필요

### 3.2 제외 (2차 이후 검토)
- 다중 운영자/역할별 권한 체계 — admin은 계속 유니 1명 (glossary/PRD.md 3.2와 동일 원칙 계승)
- 자료실 전용 "신고" 버튼(학생이 특정 자료에 바로 신고를 남기는 UI) — 1차는 문의함(유형=신고)으로 대체, 자료에 종속된 전용 신고 플로우는 2차 검토
- 문의 답변에 대한 이메일/푸시 알림 — 1차는 학생이 문의함에 재접속해 직접 확인
- 공지사항 대상 세분화(특정 대상 타겟팅), 예약 발행
- 관리자 활동 로그(승인/삭제 이력의 감사 기록) — admin이 1명뿐이라 1차 우선순위 낮음
- 운영 통계 대시보드(가입자 수, 업로드 추이 등)

---

## 4. 기능 요구사항

### 4.1 관리자 진입 / 가드
- 헤더 등 일반 UI에는 노출하지 않음 — `role=admin` 계정으로 로그인한 유니에게만 별도 진입 지점(예: 프로필 메뉴 안 "관리자창") 노출
- `/admin` 하위 모든 라우트는 서버 사이드에서 `role=admin` 검증, 실패 시 404 또는 홈으로 리다이렉트(일반 사용자에게 관리자 영역의 존재 자체를 노출하지 않는 방향 권장 — 오픈 이슈)
- 좌측 또는 상단에 5개 섹션 내비게이션: 용어 승인 / 이론가 관리 / 자료실 모더레이션 / 공지사항 / 문의함

### 4.2 용어집 승인 대기 (기존 기능 이관)
- glossary/PRD.md 4.5의 내용을 그대로 수행하는 화면. 신규 요구사항 없음
- 목록: 대기중(pending) 용어를 제출일순으로 표시, 클릭 시 상세 편집 화면
- 편집 화면: 학생이 제출한 용어명+정의(참고용, 읽기전용 표시) 옆에 원문 정의/원문 출처/내 정의/카테고리(9종 중 선택)/관련 이론가(이론가 레코드 검색·연결)/영문 원어/관점별 정의(선택) 입력 필드
- 액션: 게시(published로 전환) / 반려(목록에서 제거)

### 4.3 이론가 프로필 관리 (기존 기능 이관)
- theorist/PRD.md 4.5, 5장의 내용을 그대로 수행하는 화면
- 목록: 전체 이론가(draft 포함) 표시, draft/published 상태 뱃지로 구분
- 작성/수정 화면: theorist/PRD.md 5장 데이터 모델의 전체 필드(이름, 생몰년, 국적, 학파, 초상화, 생애, 사상 리스트, 대표 저서, 인용구) 입력
- 액션: 임시저장(draft) / 게시(published) / 삭제

### 4.4 자료실 모더레이션 (기존 오픈 이슈 이관)
- archive/PRD.md의 "익명 처리 원칙의 예외" 오픈 이슈에 대한 답 — 관리자창에서는 자료의 `owner_id`를 실제로 확인 가능(일반 UI에서는 계속 익명 처리 유지)
- 문의함에서 신고 유형 문의가 들어오면 해당 자료 링크를 통해 바로 이동, 또는 관리자창 내 검색으로 특정 자료를 직접 조회
- 자료 상세에서 확인 가능한 정보: 제목, 유형(파일/텍스트), 실제 업로더(이름/이메일), 공개 여부, 태그, 등록일
- 액션: 비공개로 강제 전환 / 완전 삭제(둘 다 가능, 삭제는 되돌릴 수 없다는 확인 필요)

### 4.5 공지사항 (신규)
- 관리자창에서 제목+본문으로 작성, 수정·삭제 가능
- 게시된 공지사항은 전체 학생에게 노출(노출 위치는 오픈 이슈 — 헤더 배너 / 홈 전용 섹션 등)
- 최신순 목록, 학생 쪽에서는 읽기 전용

### 4.6 문의함 (신규)
- 학생이 제출한 문의를 최신순으로 표시, 상태(대기중/답변완료) 필터
- 문의 유형(질문/신고/제안 등) 뱃지 표시
- 문의 상세에서 학생이 남긴 내용 확인 후 답변 작성 → 저장 시 상태가 답변완료로 전환되고 학생에게 전달(전달 방식은 오픈 이슈)
- 학생 쪽 "문의하기" 폼: 유형 선택 + 내용 입력(제목 없이 본문만, 간단하게 — 용어 제안 폼과 동일한 원칙)

---

## 5. 데이터 모델 (초안)

```
Announcement
- id
- title (string, required)
- content (text, required)
- author_id (FK → User, admin)
- created_at, updated_at

Inquiry (문의)
- id
- user_id (FK → User, required)
- type (enum: question / report / suggestion)
- content (text, required)
- related_material_id (FK → Material, nullable — 자료실 신고일 때만 사용, 오픈 이슈)
- status (enum: pending / answered, default: pending)
- answer (text, nullable)
- answered_at (nullable)
- created_at
```

> 용어집(Term.status), 이론가(Theorist.status), 자료실(Material.visibility, Material.owner_id)은 이미 각 기능의 데이터 모델에 정의돼 있으므로 관리자창을 위한 별도 테이블을 새로 만들지 않는다. 관리자창은 이 필드들을 조작하는 화면일 뿐이다.

---

## 6. 의존성

- **계정 시스템**: `role=admin` 판별 필요 (glossary/PRD.md 5장 `User.role` 필드를 앱 전역에서 재사용)
- **용어집 데이터 모델**: `Term.status`(pending/published/rejected) 조작
- **이론가 데이터 모델**: `Theorist.status`(draft/published) 조작 — theorist/PRD.md 7장 오픈 이슈("상태값을 둘지 여부")가 이 문서로 인해 "둔다"로 확정되는 셈이므로 theorist/PRD.md에도 반영 필요
- **자료실 데이터 모델**: `Material.owner_id`, `Material.visibility` 조작 — archive/PRD.md 오픈 이슈("관리자 화면이 필요한지")가 이 문서로 인해 "필요하다"로 확정되는 셈이므로 archive/PRD.md에도 반영 필요

---

## 7. 오픈 이슈

- `/admin` 접근 실패 시 404로 위장할지, 로그인 유도 페이지를 보여줄지(관리자 영역 존재 자체를 숨길지 여부)
- 자료실 신고를 문의함(type=report)으로 통합할지, 자료 상세에 별도 신고 버튼을 만들지(3.2에서는 1차 제외로 정리했으나 최종 확정 필요)
- 공지사항 노출 위치와 "읽음" 처리 여부(안 읽은 공지 뱃지 등)
- 문의 유형 목록 확정(질문/신고/제안 외 추가 필요 여부)
- 문의 답변을 학생이 어디서 확인하는지(마이페이지 내 탭 신설 필요 여부, 알림 아이콘 등) — 3.2에서 이메일/푸시는 제외했지만 인앱 확인 경로는 확정 필요
- 승인 대기 용어·문의가 많아질 경우 정렬/필터(제출일순 외 검색 등) 필요 여부
- 자료실 삭제 처리 시 업로더 본인에게 삭제 사유를 알릴지 여부
