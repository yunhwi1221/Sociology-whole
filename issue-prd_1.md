# PRD — 시사 연결 (Issue)

작성일: 2026-08-28
상태: Draft v0.1
상위 문서: 루트 `PRD.md`, 루트 `design.md` (본 문서는 전체 앱 PRD의 하위 feature 문서. 상충되는 내용이 있으면 루트 문서가 우선하며, 이 경우 루트 문서를 함께 업데이트할 것)
연관 문서: `glossary/PRD.md`, `theorist/PRD.md` (관련 개념·이론가 태그 연동), `admin/PRD.md` (콘텐츠 관리 메뉴 확장 필요 — 6장 의존성 참고)

> **v0.2 개정 (2026-08-28)**: 유니와 함께 아래 항목을 확정했다.
> - 콘텐츠 생성 방식: **AI 초안 + 유니 검수** 후 게시
> - 뉴스 API: **네이버 API Hub의 뉴스 검색 API**
> - 무료 제한: **로그인 사용자 하루 3개**, 재열람은 무료(새 아티클만 카운트), 자정(KST) 리셋
> - 비로그인 사용자: 상세 진입 자체를 막고 **로그인 유도**
> - 북마크: **상세를 열람(카운트 소진)한 아티클만** 북마크 가능
> - 정치적 민감성: **특정 정당·정치인은 다루지 않고, 구조적·사회현상 중심**으로만 다룸
> - 목록 필터(이론가/개념): **1차 범위에 포함**
>
> 실제 결제(구독) 연동 시점·가격 정책은 여전히 이번 범위 밖이며, 7장에 남겨둔 것이 유일하게 남은 정책적 오픈 이슈다.

---

## 1. 개요

### 1.1 한 줄 정의
추상적인 사회학 이론(개념·이론가)을 오늘의 실제 뉴스에 적용해보는 짧은 해석 콘텐츠 — "이 뉴스, 이 이론으로 보면 어떻게 읽힐까?"를 매번 새로운 이슈로 보여주는 코너.

### 1.2 배경
용어집과 이론가 프로필은 "개념이 무엇인지"를 정의하는 레퍼런스형 콘텐츠다. 학생들이 실제로 어려워하는 지점은 그다음 단계 — "이 개념을 실제 현실에 어떻게 적용하는가"이다. 시사 연결은 이 간극을 메우는 기능으로, 매일 쏟아지는 뉴스 중 사회학적으로 흥미로운 사례를 골라 "질문형 제목 + 뉴스 요약 + 이론적 해석"의 짧은 카드로 제공한다.

용어집·이론가가 "고정된 정답"을 다루는 반면, 시사 연결은 "이론을 도구 삼아 현실을 해석해보는 연습"에 가깝다. 그래서 이 기능은 앱 안에서 유일하게 다음 두 성격을 동시에 가진다.
- 콘텐츠가 **시간이 지나면 낡는다**(뉴스 기반이므로 지속적인 갱신이 필요) — 용어집·이론가처럼 한 번 잘 써두면 오래가는 콘텐츠가 아님
- 앱에서 **처음으로 무료/프리미엄 구분**이 생기는 기능 — 지금까지는 모든 기능이 로그인 여부로만 갈렸지만, 시사 연결은 로그인 사용자 안에서도 이용량 제한이 생김

### 1.3 목적
- 이론을 "암기 대상"이 아니라 "현실을 읽는 도구"로 체감하게 하는 학습 경험 제공
- 매일/매주 새로운 이슈를 올려 학생이 주기적으로 재방문할 이유를 만듦
- 용어집·이론가 콘텐츠로 트래픽을 되돌리는 허브 역할(관련 개념·이론가 태그로 연결)
- 앱의 첫 유료 전환 포인트(무료 3회 제한)를 자연스럽게 도입

---

## 2. 타겟 사용자 & 핵심 시나리오

| 사용자 | 시나리오 |
|---|---|
| 이론 적용 연습을 하고 싶은 학생 | 헤더 "시사 연결" 진입 또는 홈 미리보기 섹션에서 카드 목록 훑어봄 → 관심 가는 질문형 제목 클릭 → (비로그인이면 로그인 유도) → 상세에서 뉴스 요약 + 이론적 해석 읽음 → 태그 클릭해 관련 이론가/용어로 이동 |
| 매일 들어오는 헤비 유저 | 하루 3개를 이미 다 읽음 → 4번째 아티클 클릭 시 "오늘의 무료 열람을 모두 사용했어요" 안내 → 내일 다시 오거나 프리미엄 안내(준비 중) 확인 |
| 시험 대비 중인 학생 | 특정 이론가(예: 뒤르켐)로 태그된 시사 연결 글만 모아보고 싶어함 → 이론가 상세 페이지 또는 시사 연결 목록에서 이론가/개념으로 필터 |
| 운영자(유니) | 관리자 화면에서 키워드로 뉴스 검색 → 후보 기사 중 사회학적으로 다룰 만한 것 선택 → AI 초안 생성 → 사실관계·해석 검수 및 수정 → 게시 |

---

## 3. 범위 (1차 버전)

### 3.1 포함
- 진입 구조: 사이트 헤더에 "시사 연결" 메뉴 추가 + 홈 화면에 최신 카드 미리보기 섹션(현재 랜딩에 이미 노출된 형태)
- 목록(전체보기) 페이지: 카드형 미리보기(질문형 제목, 뉴스 한 줄 요약, 관련 이론가/개념 태그, NEW 배지), 최신순 정렬
- 상세 페이지: 카드 안/밖 분리 패턴 계승 — 카드 안(질문형 제목, 원문 뉴스 정보·출처 링크, 이론적 해석 요약), 카드 밖(해석 본문 전체, 관련 이론가/용어 칩)
- **무료/프리미엄 이용 제한**: 로그인 사용자 기준 하루 3개 아티클까지 상세 전체 열람 가능(4.5 참고), 목록·카드 미리보기(제목+요약)는 제한 없이 무제한 열람
- 비로그인 사용자: 카드 목록(제목+요약+태그)까지는 열람 가능, 상세 전체 열람은 로그인 필요
- 뉴스 소재 수집: 네이버 뉴스 검색 API 연동, 관리자 전용 검색 도구(4.6 참고, 오픈 이슈)
- AI 초안 생성: 관리자가 기사를 선택하면 LLM이 질문형 제목·요약·이론적 해석 초안을 생성, 관리자 검수 후 게시(4.6 참고, 오픈 이슈)
- 관련 이론가/용어 태그: 용어집·이론가 데이터를 참조해 연결, 클릭 시 각 상세 페이지로 이동
- 북마크: 기존 패턴(용어·이론가·자료·게시글)과 동일하게 지원
- NEW 배지: 게시 후 일정 기간 노출(정확한 기간은 오픈 이슈)

### 3.2 제외 (2차 이후 검토)
- **실제 결제(구독) 연동** — 이번 범위는 "무료 3회 제한 + 프리미엄 안내 UI"까지만 다루고, PG 연동·결제·구독 갱신·환불 등은 범위 밖(7장 오픈 이슈 참고). 프리미엄 CTA는 클릭 시 "준비 중입니다" 또는 대기 리스트 신청 정도로만 처리
- 학생의 시사 연결 콘텐츠 제안 기능 (이론가와 동일하게 유니 전용 콘텐츠 — 학생 제안 없음)
- 완전 자동 발행(사람 검수 없는 AI 파이프라인) — 반드시 관리자 검수를 거쳐야 게시됨
- 댓글/토론 기능
- 개인화 추천(관심 이론가/카테고리 기반 알고리즘 추천)
- 다국어 지원
- 뉴스 원문 전체 게재(항상 출처 링크 + 요약만 사용, 저작권 문제 회피)

---

## 4. 기능 요구사항

### 4.1 진입 구조
- 사이트 헤더에 "시사 연결" 메뉴 추가 → 전체 목록 페이지로 이동
- 홈 화면 미리보기 섹션(현재 랜딩에 이미 존재하는 형태): 최신 게시물 중 일부(예: 3~4개)를 카드로 노출 + "더보기" 링크로 전체 목록 이동

### 4.2 목록(전체보기) 페이지
- 카드형 미리보기, 카드당 표시 요소:
  - NEW 배지(게시 후 일정 기간만, 4.7 참고)
  - 질문형 제목 (예: "저출산 현상을 뒤르켐의 아노미 개념으로 보면?")
  - 뉴스 요약 1~2문장 (카드 미리보기용 짧은 요약, 상세의 해석 본문과는 다른 필드)
  - 관련 이론가/개념 태그(칩, 최대 2~3개 노출 — 정확한 개수 및 우선순위는 오픈 이슈)
- 정렬: 최신순 고정(커뮤니티와 동일 원칙 — 인기순·조회순 없음, 화제성으로 소비되는 구조를 만들지 않음)
- **필터(1차 범위 포함, 확정)**: 관련 이론가 또는 개념으로 필터링. 시험 대비 학생이 "뒤르켐으로 태그된 글만 보기" 식으로 좁혀볼 수 있게 함 — `IssueArticle.related_theorists` / `related_terms` 매핑을 그대로 필터 기준으로 재사용하므로 구현 부담이 크지 않음
- 카드 클릭 시 상세 페이지로 이동. 비로그인 사용자가 클릭하면 로그인 유도(목록 자체는 열람 가능)
- 카드에 노출하는 태그는 **최대 3개**(이론가 1개 우선 + 개념 최대 2개)로 제한. 관련 이론가/개념이 더 많으면 상세 페이지의 "카드 밖" 영역에서 전부 확인
- 목록 레이아웃은 카드형 그리드(현재 랜딩에 노출된 형태와 동일 — PC 2~3열, 모바일 1열)로 확정. 세부 반응형 규칙은 `issue/design.md`에서 별도 정리

### 4.3 상세 페이지

용어집·이론가와 동일하게 **카드 영역**과 **카드 밖 영역**으로 나눈다.

**카드 영역** (핵심 정보 — 여기까지만 읽어도 "이 이슈를 이 이론으로 어떻게 보는지" 파악 가능해야 함):
- 질문형 제목 (Noto Serif KR)
- 원문 뉴스 정보: 언론사명, 게재일, 원문 링크(새 탭으로 이동, 출처 명시 — 저작권 대응)
- 뉴스 요약 (카드 미리보기와 동일 필드 재사용 또는 상세용으로 조금 더 길게, 오픈 이슈)
- 이론적 해석 도입부 또는 핵심 한 문단 (카드 안에서 가장 먼저 읽히는 해석)
- 북마크 버튼(로그인 필요, 비로그인 시 클릭하면 로그인 유도)

**카드 밖 영역**:
- 이론적 해석 본문 전체 (핵심 콘텐츠 — 이 이슈를 해당 이론/개념으로 왜, 어떻게 해석할 수 있는지 상세 서술)
- 관련 이론가 (칩, 클릭 시 이론가 상세로 즉시 이동 — 이론가의 "관련 용어" 칩과 동일하게 팝업 경유 없이 바로 이동. 이론가 프로필이 이미 존재하는 기능이므로)
- 관련 용어 (칩, 클릭 시 용어집 상세로 즉시 이동)

### 4.4 상세 열람 → 로그인 & 무료 제한 게이트 (확정)

- **비로그인 사용자**가 상세 페이지 진입을 시도하면 **상세 진입 자체를 막고 로그인 화면/모달로 유도**한다. 카드 밖 영역은 물론 카드 영역도 보여주지 않는다(목록의 카드 미리보기까지만 열람 가능) — 블러 미리보기 같은 중간 단계는 두지 않아 구현을 단순하게 유지
- 로그인 사용자가 상세 페이지 진입 시도 → 4.5의 일일 제한 로직 확인 후 통과/차단

### 4.5 무료 이용 제한 (하루 3회, 확정)

- **기준**: 로그인 사용자 1명당 **하루 3개** 아티클까지 상세 전체 열람 가능
- **집계 단위**: "고유 아티클" 기준 — 오늘 이미 열람한 아티클을 다시 보는 것은 **카운트에 포함하지 않는다**(재열람은 무료). 한 번 읽은 글을 복습하려고 다시 들어갔다가 무료 횟수가 깎이는 억울함을 방지하기 위한 결정
- **리셋 시점**: **매일 자정(KST) 기준**으로 카운트 초기화
- **카운트 시점**: 목록·카드 미리보기(제목+요약)는 제한 대상이 아니며, **상세 페이지의 카드 밖 영역(해석 본문)을 펼쳐 보는 시점**에 1회로 카운트
- **제한 도달 시 UX**: 4번째(오늘 처음 보는) 아티클 상세 진입 시도 → 안내 화면 노출
  - 문구 예시: "오늘의 무료 열람 3회를 모두 사용했어요. 내일 다시 만나요."
  - 프리미엄 CTA 버튼 노출(현재는 실제 결제 없음 — 클릭 시 "프리미엄은 준비 중입니다" 안내 또는 관심 등록 폼, 3.2 참고)
- **비로그인 사용자**: 상세 자체를 열람할 수 없으므로 이 제한과 무관 (로그인 유도가 우선)

### 4.6 콘텐츠 생성 파이프라인 (관리자 전용, 확정: AI 초안 + 유니 검수)

이론가와 마찬가지로 학생 제안 없이 유니가 전담하되, 뉴스 기반 콘텐츠 특성상 매번 처음부터 쓰기보다 AI가 초안을 만들고 유니가 검수·게시하는 흐름으로 확정한다.

1. **소재 검색**: 관리자 화면에서 키워드(예: "저출산", "청년 실업") 입력 → **네이버 API Hub의 뉴스 검색 API** 호출 → 후보 기사 목록(제목, 언론사, 게재일, 원문 링크, 짧은 스니펫) 반환
2. **기사 선택**: 관리자가 사회학적으로 다룰 만한 기사를 선택. 이때 4.6.1의 정치적 민감성 원칙을 1차 필터로 적용
3. **AI 초안 생성**: "초안 생성" 버튼 클릭 → LLM이 (a) 질문형 제목 후보, (b) 뉴스 요약, (c) 이론적 해석 초안, (d) 관련 이론가/개념 추천 후보를 함께 생성. 추천 후보는 자유 텍스트가 아니라 기존 `Theorist`/`Term` 테이블에서 매칭되는 레코드만 후보로 제시(존재하지 않는 이론가·용어를 지어내지 않도록)
4. **검수 및 확정**: 관리자가 다음을 검토·수정
   - 뉴스 인용의 사실관계가 원문과 일치하는지
   - 이론적 해석이 논리적으로 타당한지(억지 연결 아닌지)
   - 4.6.1 정치적 민감성 원칙을 지켰는지
   - AI가 추천한 관련 이론가/개념이 실제로 적절한지(추가·제거 가능)
   - AI 초안은 반드시 이 검수를 거쳐야 게시 가능하다 — 사람 검수 없는 자동 발행은 하지 않는다(3.2 참고)
5. **게시**: 상태를 `draft` → `published`로 전환, 게시 시점부터 NEW 배지 기간(4.7) 시작

#### 4.6.1 정치적 민감성 원칙 (확정)
- **특정 정당·정치인이 중심이 되는 이슈는 다루지 않는다**(예: 특정 정당의 정책 발표, 특정 정치인 관련 논란)
- 대신 **구조적·사회현상 중심**의 이슈만 다룬다(예: 저출산, 청년 실업, 세대 갈등, 젠트리피케이션, 플랫폼 노동 등) — 이런 이슈의 배경에 정치적 논쟁이 있더라도, 다루는 각도는 어디까지나 "이 사회현상을 이론으로 어떻게 해석하는가"로 한정한다
- 이 원칙은 관리자가 소재를 선택하는 2단계에서 1차로 적용하고, 4단계 검수에서 다시 한번 확인한다

### 4.7 NEW 배지 (확정)
- 게시 후 **7일간** 카드에 NEW 배지 노출
- 기간 경과 시 자동으로 배지 제거(별도 관리자 조작 불필요, `published_at` 기준 서버에서 계산)

### 4.8 북마크 (확정)
- 로그인 사용자만 사용 가능
- **상세를 열람(4.5 기준 카운트가 소진된) 아티클만 북마크 가능** — 목록 카드 단계에서는 북마크할 수 없다. 용어집·이론가·자료실의 북마크가 모두 "상세를 본 콘텐츠"를 대상으로 하는 것과 일관된 원칙
- 마이페이지 "북마크한 글" 탭에서 기존 북마크(용어·이론가·자료·게시글)와 함께 확인

---

## 5. 데이터 모델 (초안)

```
IssueArticle
- id
- news_title (string, required)              // 원문 뉴스 제목
- news_url (string, required)                 // 원문 링크 (저작권 대응 — 원문 전체 게재 금지)
- news_source (string, required)              // 언론사명
- news_published_at (date, required)
- question_title (string, required)           // 질문형 제목, 예: "저출산 현상을 뒤르켐의 아노미 개념으로 보면?"
- summary (text, required)                    // 카드 미리보기용 짧은 요약 (1~2문장)
- interpretation (text, required)             // 이론적 해석 본문 전체 (카드 밖)
- related_theorists (array of theorist_id, FK → Theorist)
- related_terms (array of term_id, FK → Term)
- ai_generated (boolean, default true)        // AI 초안 여부 (내부 운영 참고용)
- status (enum: draft / published)
- published_at (datetime, nullable)
- created_at, updated_at

IssueArticleView (일일 무료 열람 집계용)
- id
- user_id (FK → User)
- issue_article_id (FK → IssueArticle)
- viewed_at (date)                            // KST 기준 날짜만 저장해 "오늘 몇 개 봤는지" 집계에 사용
- created_at (datetime, 최초 열람 시각)

IssueArticleBookmark
- user_id (FK → User)
- issue_article_id (FK → IssueArticle)
- created_at

User (확장 제안)
- plan (enum: free / premium, default: free)  // 지금은 전부 free, 향후 결제 연동 시 사용할 필드만 미리 준비
```

> `IssueArticleView`는 user_id + issue_article_id 조합에 유니크 제약을 걸어, 같은 아티클을 오늘 여러 번 열람해도 1건으로 집계되게 한다. "오늘 3개까지" 판단은 `viewed_at = 오늘(KST)`인 고유 `issue_article_id` 개수를 세는 방식으로 구현한다(4.5 참고).

---

## 6. 의존성

- **계정 시스템**: 상세 열람 집계와 로그인 게이트를 위해 로그인 필수(`features/auth`에 의존, 기존 구글 로그인 재사용)
- **용어집·이론가 데이터**: 관련 개념/이론가 태그 연동을 위해 `Term`, `Theorist` 테이블 참조. 신규 스키마 변경은 필요 없음(참조만 함)
- **관리자 시스템**: `admin/PRD.md`에 아직 "시사 연결 관리" 메뉴가 없음 — 15~17장(용어/이론가/자료 관리)과 동일한 패턴(목록/검색/추가/수정/공개-비공개/삭제)으로 신규 메뉴 추가 필요, 관리자 로그 대상(24장)에도 "시사 연결 추가/수정/삭제/게시" 항목 추가 필요
- **외부 뉴스 API**: 네이버 API Hub의 뉴스 검색 API(확정) — API 키 발급 및 호출 한도 확인 필요
- **LLM API**: AI 초안 생성용 — 이미 앱에서 다른 용도로 Claude API 등을 쓰고 있다면 재사용, 아니라면 신규 연동 필요
- **(향후) 결제/구독 시스템**: 이번 범위에서는 실제 연동하지 않지만, `User.plan` 필드를 미리 준비해두면 이후 결제 기능 추가 시 마이그레이션 부담이 줄어듦

---

## 7. 오픈 이슈

v0.1의 오픈 이슈는 유니와 함께 대부분 확정했다(4장 각 절의 "확정" 표시 및 문서 상단 v0.2 개정 요약 참고). 아래는 이번 PRD 범위에서 의도적으로 미룬, 진짜로 남은 이슈다.

- **프리미엄 결제 연동**: 도입 시점, 가격 정책, 결제 수단(PG) 선정은 이번 범위 밖. 지금은 하루 3회 제한 UI와 "프리미엄 준비 중" CTA까지만 구현하고, `User.plan` 필드만 미리 준비해둔다(6장 의존성 참고). 실제 결제가 필요해지는 시점에 별도 PRD로 다룬다
- **네이버 API Hub 뉴스 검색 API의 응답 정보 범위 확인**: 이 API는 기사 제목·요약(스니펫)·언론사·게재일·원문 링크만 제공하고 본문 전체는 제공하지 않는다. 4.6의 AI 초안 생성이 스니펫만으로 충분히 정확한 해석 초안을 만들 수 있는지는 실제 연동 후 품질을 보며 검증이 필요하다 — 부족하다면 관리자가 원문 링크를 직접 열어 본문을 참고해 초안을 보완하는 보조 절차가 필요할 수 있음
- **뉴스 저작권**: 원문 전체 게재 금지·출처 링크 필수 원칙은 확정했으나, 그 외 추가로 확인해야 할 법적 이슈(예: 스니펫 인용 범위, 언론사별 API 이용약관)가 있는지는 실제 계약/약관 검토 필요
- **관리자 화면의 소재 검색 UX 세부사항**: 키워드 검색 결과가 너무 많거나 적을 때의 처리, 이미 시사 연결로 다룬 적 있는 기사의 중복 알림 여부 등은 실제 관리자 도구를 설계하면서 구체화 필요
