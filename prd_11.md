# Sociology Whole 관리자 전용 PRD

## 1. 문서 개요

### 1.1 문서 목적

본 문서는 Sociology Whole 서비스의 관리자 전용 기능을 정의한다.
관리자는 서비스 운영에 필요한 최소한의 회원 관리, 콘텐츠 관리, 학습 활동 통계, 자료 관리 및 시스템 운영 기능을 수행할 수 있다.
관리자 기능은 개인정보 보호를 최우선으로 하며, 서비스 운영에 필요하지 않은 개인정보의 수집·열람·저장을 제한한다.

본 PRD는 **현재 Sociology Whole에 실제 구현되어 있는 기능을 우선 기준으로 작성한다.**
현재 구현되지 않은 기능은 관리자 기능으로 임의 추가하지 않는다.

---

# 2. 기본 원칙

1. 최소한의 개인정보만 관리자에게 노출한다.
2. 관리자가 일반 사용자의 비밀번호를 조회할 수 없도록 한다.
3. 인증 토큰 및 Secret Key를 관리자 화면에 노출하지 않는다.
4. 다른 사용자의 개인정보를 불필요하게 열람할 수 없도록 한다.
5. 학습 데이터는 가능한 경우 집계된 통계 형태로 제공한다.
6. 관리자 권한과 일반 사용자 권한을 명확하게 분리한다.
7. 관리자 페이지 접근뿐 아니라 DB 접근에도 권한 검증을 적용한다.
8. Supabase RLS를 활용하여 DB 수준에서 관리자 권한을 보호한다.
9. 관리자에 의한 주요 변경 작업은 로그로 기록한다.
10. 현재 존재하지 않는 기능을 관리자 페이지에 임의로 추가하지 않는다.

---

# 3. 관리자 권한 모델

## 3.1 USER

일반 사용자.

### 접근 가능

* 메인 서비스
* 사회학 용어
* 사회학자/이론가
* 마이페이지
* 개인 학습 기능
* 개인 자료 관련 기능

### 접근 불가

* 관리자 페이지
* 다른 사용자의 개인정보
* 관리자 통계
* 관리자 콘텐츠 관리
* 관리자 로그

---

# 4. ADMIN

서비스 관리자.

### 접근 가능

* 관리자 대시보드
* 회원 관리
* 사회학 용어 관리
* 사회학자/이론가 관리
* 학습자료 관리
* 서비스 이용 통계
* 공지사항 관리
* 신고 관리
* 관리자 로그

관리자 권한은 일반 사용자와 반드시 분리한다.

---

# 5. 개인정보 보호

## 5.1 관리자에게 제공 가능한 개인정보

서비스 운영에 필요한 최소한의 정보만 제공한다.

### 기본 정보

* 사용자 이름
* 이메일
* 가입일
* 마지막 접속일
* 계정 상태
* 사용자 권한

---

## 5.2 관리자에게 제공하지 않는 정보

다음 정보는 관리자 화면에서 직접 조회하지 않는다.

* 비밀번호
* 인증 토큰
* Supabase Auth Secret
* Supabase Service Role Key
* API Secret Key
* 불필요한 개인정보
* 전화번호
* 주소
* 주민등록번호
* 결제정보

사용자가 업로드한 자료 역시 운영상 필요한 범위를 넘어 불필요하게 열람하지 않는다.

---

# 6. 개인정보 마스킹

회원 목록에서는 이메일을 기본적으로 마스킹한다.

예:

```text
abc123@example.com
```

↓

```text
abc***@example.com
```

관리자에게 전체 이메일 주소가 반드시 필요한 운영 상황이 아니라면 마스킹된 상태를 유지한다.

---

# 7. 관리자 페이지 구조

```text
/admin
│
├── Dashboard
│
├── Users
│   ├── User List
│   └── User Detail
│
├── Contents
│   ├── Glossary
│   ├── Theorists
│   └── Learning Materials
│
├── Analytics
│   ├── User Analytics
│   └── Content Analytics
│
├── Reports
│
├── Notices
│
└── Admin Logs
```

### 중요

현재 구현되지 않은 퀴즈 관리 메뉴는 포함하지 않는다.

---

# 8. 관리자 로그인

## 8.1 인증

관리자 페이지는 기존 Sociology Whole의 Supabase Auth 인증 시스템을 사용한다.
관리자 전용 인증 시스템을 별도로 만들지 않는다.

---

## 8.2 관리자 권한 확인

관리자 페이지 접근 과정:

```text
사용자 접속
↓
Supabase 로그인 상태 확인
↓
사용자 계정 확인
↓
관리자 권한 확인
↓
관리자 페이지 접근
```

관리자가 아닌 사용자가 `/admin`에 접근하면 관리자 페이지를 표시하지 않는다.

권장 처리:

```text
관리자 권한 없음
↓
403 또는 메인 페이지 이동
```

---

# 9. 관리자 대시보드

## 9.1 목적

관리자가 서비스의 현재 상태를 한눈에 확인할 수 있도록 한다.

---

## 9.2 회원 KPI

다음 정보를 표시한다.

* 전체 회원 수
* 신규 가입자 수
* 활성 사용자 수

기간:

* 오늘
* 최근 7일
* 최근 30일

---

## 9.3 콘텐츠 KPI

다음 정보를 표시한다.

* 전체 사회학 용어 수
* 전체 사회학자/이론가 수
* 전체 학습자료 수

---

## 9.4 학습 활동 KPI

현재 실제 구현된 학습 관련 데이터를 기준으로 제공한다.

예:

* 전체 학습 활동
* 최근 학습 활동
* 콘텐츠 조회/사용량

단, 실제 DB에 존재하지 않는 통계 데이터를 임의로 생성하지 않는다.

---

# 10. 회원 관리

## 10.1 회원 목록

관리자는 서비스에 가입한 회원 목록을 확인할 수 있다.

### 표시 항목

| 항목     | 표시     |
| ------ | ------ |
| 이름     | O      |
| 이메일    | 기본 마스킹 |
| 가입일    | O      |
| 마지막 접속 | O      |
| 계정 상태  | O      |
| 권한     | O      |

---

## 10.2 회원 검색

다음 기준으로 검색할 수 있다.

* 이름
* 이메일

검색 결과에도 개인정보 최소 노출 원칙을 적용한다.

---

# 11. 회원 상세

관리자가 회원을 선택하면 다음 정보를 확인한다.

## 기본 정보

* 이름
* 이메일
* 가입일
* 마지막 접속일
* 계정 상태
* 권한

## 학습 활동 요약

현재 서비스에서 실제로 기록하고 있는 데이터만 표시한다.

예:

* 학습 활동 수
* 콘텐츠 이용 활동
* 북마크 등 현재 구현된 개인 기능의 이용량

존재하지 않는 데이터를 추정하거나 생성하지 않는다.

---

# 12. 회원 상태 관리

관리자는 필요한 경우 계정 상태를 관리할 수 있다.

### 기능

* 계정 상태 확인
* 계정 비활성화
* 계정 활성화

계정 상태 변경 시 관리자 로그를 남긴다.

---

# 13. 관리자 권한 관리

관리자는 사용자의 관리자 권한을 관리할 수 있다.

### 기능

* 일반 사용자 → 관리자
* 관리자 → 일반 사용자

단, 관리자 권한 변경은 중요한 보안 작업이므로 반드시 관리자 로그에 기록한다.

---

# 14. 회원 삭제

회원 삭제는 초기 관리자 기능에서 신중하게 처리한다.

권장 방식:

```text
회원 삭제
↓
확인 모달
↓
삭제 대상 확인
↓
삭제
↓
관리자 로그 기록
```

가능하면 서비스 정책과 개인정보 보존 정책을 고려하여 즉시 물리 삭제보다 계정 비활성화 방식을 우선 검토한다.

---

# 15. 사회학 용어 관리

현재 Sociology Whole의 `glossary` 콘텐츠를 관리한다.

## 15.1 기능

* 목록 조회
* 검색
* 추가
* 수정
* 공개/비공개
* 삭제

---

## 15.2 관리 데이터

실제 DB 구조에 존재하는 필드만 사용한다.

예상 데이터:

```text
term
definition
description
category
status
created_at
updated_at
```

DB에 존재하지 않는 필드는 개발 과정에서 임의로 추가하지 않는다.

---

# 16. 사회학자 / 이론가 관리

현재 `theorists` 콘텐츠를 관리한다.

## 16.1 기능

* 목록 조회
* 검색
* 추가
* 수정
* 공개/비공개
* 삭제

---

## 16.2 관리 정보

현재 콘텐츠 구조에 존재하는 정보를 기준으로 관리한다.

예:

* 이름
* 생몰년
* 주요 이론
* 핵심 개념
* 대표 저서
* 설명
* 관련 콘텐츠

실제 DB 스키마를 먼저 확인한 후 구현한다.

---

# 17. 학습자료 관리

현재 Sociology Whole에서 사용되는 학습자료 기능을 관리한다.

## 17.1 기능

* 자료 목록
* 자료 검색
* 자료 상태 확인
* 공개/비공개
* 삭제

---

## 17.2 표시 정보

가능한 경우 다음 정보를 표시한다.

* 자료 제목
* 등록자
* 등록일
* 파일 유형
* 상태

개인정보가 포함된 자료의 원문은 관리자에게 불필요하게 노출하지 않는다.

---

# 18. 학습자료 개인정보 보호

사용자가 자료를 업로드할 수 있는 기능이 있는 경우 특히 주의한다.

관리자는 자료를 운영 목적으로 관리할 수 있지만, 단순한 호기심이나 통계 확인을 위해 개인 자료의 내용을 열람해서는 안 된다.

가능한 경우 다음과 같이 관리한다.

```text
자료 존재 여부
↓
등록자
↓
등록일
↓
상태
```

원문 열람은 신고 또는 운영상 필요한 경우에 한정한다.

---

# 19. 서비스 분석

## 19.1 목적

서비스 이용 현황을 파악하고 Sociology Whole의 콘텐츠와 학습 경험을 개선하기 위한 기능이다.
사용자를 감시하기 위한 목적이 아니다.

---

# 20. 사용자 분석

개인 식별을 최소화한 집계 데이터를 제공한다.

### 기본 지표

* 전체 회원
* 신규 회원
* 활성 사용자
* 재방문 사용자
* 기간별 사용자 변화

### 기간

* 오늘
* 7일
* 30일
* 사용자 지정 기간

---

# 21. 콘텐츠 분석

콘텐츠가 실제로 얼마나 이용되고 있는지 확인한다.

### 사회학 용어

* 인기 용어
* 조회/사용량
* 이용 변화

### 사회학자/이론가

* 인기 이론가
* 조회/사용량
* 이용 변화

### 학습자료

* 인기 자료
* 자료 이용량

단, 실제 DB에 기록되는 데이터만 분석한다.

---

# 22. 신고 관리

신고 기능이 실제 서비스에 구현된 경우 관리자 메뉴에 포함한다.

## 신고 유형

* 잘못된 정보
* 부적절한 콘텐츠
* 저작권 문제
* 기타

## 처리 상태

```text
접수
↓
검토 중
↓
처리 완료
```

관리자는 신고 처리 결과를 기록할 수 있다.

신고 기능이 아직 구현되지 않은 경우 관리자 UI에 노출하지 않는다.

---

# 23. 공지사항 관리

관리자는 서비스 공지를 관리할 수 있다.

## 기능

* 공지 작성
* 공지 수정
* 공지 삭제
* 공개/비공개
* 상단 고정

### 선택 기능

* 예약 게시
* 공지 종료일

예약 게시 기능은 MVP에서 필수로 구현하지 않는다.

---

# 24. 관리자 로그

관리자의 중요한 작업을 기록한다.

## 기록 대상

* 관리자 로그인
* 관리자 로그아웃
* 회원 상태 변경
* 관리자 권한 변경
* 회원 삭제
* 용어 추가
* 용어 수정
* 용어 삭제
* 이론가 추가
* 이론가 수정
* 이론가 삭제
* 학습자료 상태 변경
* 학습자료 삭제
* 공지사항 작성
* 공지사항 수정
* 공지사항 삭제
* 신고 처리

---

## 로그 데이터

```text
admin_user_id
action
target_type
target_id
created_at
```

필요하지 않은 개인정보나 콘텐츠 원문은 로그에 저장하지 않는다.

---

# 25. 보안 요구사항

## 25.1 프론트엔드 관리자 검증 금지

다음과 같은 방식으로 관리자 여부를 판단하지 않는다.

```javascript
if (user.email === "admin@email.com") {
    showAdmin();
}
```

이메일 주소를 코드에 하드코딩하여 관리자 권한을 부여하지 않는다.

---

# 26. 관리자 권한 저장

관리자 권한은 DB에서 관리한다.

권장 구조:

```text
profiles
│
├── id
├── name
├── email
└── role
       ├── user
       └── admin
```

단, 실제 현재 Supabase DB 구조를 먼저 확인한 후 기존 구조와 충돌하지 않는 방식으로 구현한다.

---

# 27. Supabase RLS

관리자 데이터와 일반 사용자 데이터를 DB 수준에서 분리한다.

### 일반 사용자

```text
자신의 데이터
→ 접근 가능
```

```text
다른 사용자의 데이터
→ 접근 불가
```

### 관리자

```text
관리자에게 허용된 운영 데이터
→ 접근 가능
```

단순히 `/admin` URL 접근을 막는 것만으로 보안을 구현하지 않는다.

---

# 28. Secret Key 보호

다음 정보는 프론트엔드 코드에 포함하지 않는다.

* Supabase Service Role Key
* Supabase Secret Key
* Anthropic API Secret
* 서버용 API Key
* 기타 민감한 인증정보

브라우저에서는 공개 사용이 허용된 키만 사용한다.

---

# 29. 관리자 UI

## 29.1 기본 레이아웃

```text
┌──────────────────────────────────────┐
│ Sociology Whole Admin       관리자 ▼ │
├────────────┬─────────────────────────┤
│            │                         │
│ Dashboard  │                         │
│            │                         │
│ Users      │       Main Content      │
│            │                         │
│ Contents   │                         │
│            │                         │
│ Analytics  │                         │
│            │                         │
│ Reports    │                         │
│            │                         │
│ Notices    │                         │
│            │                         │
│ Logs       │                         │
│            │                         │
└────────────┴─────────────────────────┘
```

PC 환경을 우선한다.
기존 Sociology Whole의 디자인 시스템과 최대한 일관되게 구현한다.

---

# 30. MVP 범위

## P0 — 현재 우선 구현

* [ ] 관리자 로그인
* [ ] 관리자 권한 검증
* [ ] 관리자 전용 레이아웃
* [ ] 관리자 대시보드
* [ ] 회원 목록
* [ ] 회원 상세
* [ ] 회원 상태 관리
* [ ] 관리자 권한 관리
* [ ] 사회학 용어 관리
* [ ] 사회학자/이론가 관리
* [ ] 학습자료 관리
* [ ] 기본 서비스 통계
* [ ] 관리자 로그
* [ ] Supabase RLS

---

## P1 — 이후 구현

* [ ] 상세 사용자 분석
* [ ] 상세 콘텐츠 분석
* [ ] 신고 관리
* [ ] 공지사항 관리
* [ ] 기간별 통계
* [ ] 콘텐츠 이용률 분석

---

## P2 — 서비스 규모 확장 후

* [ ] 관리자 세부 권한
* [ ] 여러 관리자 계정
* [ ] 콘텐츠 승인 시스템
* [ ] 고급 통계
* [ ] 데이터 다운로드
* [ ] 자동 이상 행동 탐지

---

# 31. 현재 구현하지 않는 기능

다음 기능은 현재 관리자 MVP에서 구현하지 않는다.

* 퀴즈 관리
* 퀴즈 생성
* 퀴즈 수정
* 퀴즈 정답률 분석
* 퀴즈 풀이 통계
* 퀴즈 관련 KPI
* 퀴즈 관련 사용자 분석

향후 실제 서비스에 해당 기능이 추가될 경우 별도의 PRD 업데이트를 통해 관리자 기능을 추가한다.

---

# 32. 개인정보 보호 체크리스트

개발 완료 전에 다음을 확인한다.

* [ ] 비밀번호가 관리자 화면에 표시되지 않는가?
* [ ] 인증 토큰이 관리자 화면에 표시되지 않는가?
* [ ] Supabase Service Role Key가 프론트엔드에 노출되지 않는가?
* [ ] API Secret Key가 프론트엔드에 노출되지 않는가?
* [ ] 일반 사용자에게 다른 사용자의 정보가 노출되지 않는가?
* [ ] 이메일이 필요 이상으로 노출되지 않는가?
* [ ] 불필요한 개인정보를 수집하지 않는가?
* [ ] 일반 사용자가 `/admin`에 접근할 수 없는가?
* [ ] 일반 사용자가 관리자 데이터를 직접 조회할 수 없는가?
* [ ] Supabase RLS가 적용되어 있는가?
* [ ] 관리자 권한 변경이 기록되는가?
* [ ] 회원 상태 변경이 기록되는가?
* [ ] 회원 삭제가 기록되는가?
* [ ] 관리자 행동 로그에 불필요한 개인정보가 저장되지 않는가?

---

# 33. 개발 원칙

Claude Code 또는 다른 개발 도구로 구현할 때 다음 원칙을 반드시 준수한다.

### 원칙 1

**현재 코드와 DB를 먼저 분석한다.**

관리자 기능을 만들기 전에 다음을 확인한다.

* 현재 HTML 파일
* JS 파일
* CSS
* Supabase 설정
* Supabase 테이블
* 현재 Auth 구조
* 현재 RLS 정책

---

### 원칙 2

**존재하지 않는 DB 테이블이나 컬럼을 임의로 가정하지 않는다.**

예를 들어 실제 DB에 `quiz` 테이블이 없다면 관리자 페이지에서 퀴즈 기능을 만들지 않는다.

---

### 원칙 3

**기존 사용자 기능을 깨뜨리지 않는다.**

관리자 기능을 추가하면서 다음 기능에 영향을 주지 않는다.

* 로그인
* 회원가입
* 로그아웃
* 마이페이지
* glossary
* theorists
* 기존 학습 기능

---

### 원칙 4

**관리자 페이지는 기존 서비스와 분리한다.**

일반 사용자에게 관리자 UI가 노출되지 않아야 한다.

권장:

```text
/admin
```

또는

```text
admin.html
```

현재 프로젝트 구조에 가장 적합한 방식을 선택한다.

---

### 원칙 5

**보안을 UI보다 먼저 구현한다.**

구현 순서:

```text
현재 DB 구조 확인
        ↓
관리자 권한 구조 확인
        ↓
RLS 설계
        ↓
관리자 인증
        ↓
관리자 UI
        ↓
관리자 기능
        ↓
테스트
```

---

# 34. 최종 목표

Sociology Whole 관리자 시스템의 목표는 단순히 회원을 관리하는 것이 아니다.

관리자가 다음 세 가지를 수행할 수 있도록 하는 것이다.

### ① 서비스 운영

회원과 콘텐츠를 안전하게 관리한다.

### ② 서비스 분석

사용자가 어떤 콘텐츠를 이용하는지 집계 데이터로 확인한다.

### ③ 서비스 개선

이용 데이터를 바탕으로 Sociology Whole의 콘텐츠와 학습 경험을 개선한다.

최종적으로 관리자 시스템은 다음 원칙을 따른다.

> **"필요한 데이터는 충분히 제공하되, 필요하지 않은 개인정보는 수집하거나 노출하지 않는다."**

---

# 35. 최종 구현 우선순위

```text
1. 현재 Supabase DB / Auth 구조 분석
        ↓
2. 관리자 권한 구조 설계
        ↓
3. RLS 정책 설정
        ↓
4. 관리자 인증
        ↓
5. 관리자 레이아웃
        ↓
6. Dashboard
        ↓
7. 회원 관리
        ↓
8. Glossary 관리
        ↓
9. Theorists 관리
        ↓
10. 학습자료 관리
        ↓
11. Analytics
        ↓
12. Admin Logs
        ↓
13. 보안 테스트
```

**중요: 관리자 UI를 먼저 만들고 보안을 나중에 추가하지 않는다.**
관리자 기능은 반드시 DB 권한과 RLS를 먼저 설계한 후 구현한다.
